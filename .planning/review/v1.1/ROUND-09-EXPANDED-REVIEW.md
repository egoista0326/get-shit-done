# v1.1 Round 09 Expanded Review

**Date:** 2026-04-12
**Scope:** Stricter post-Round08 review requested by the user, with additional subagent lanes for Codex conversion conformance, prompt capability preservation, runtime/workstream safety, migration proof, experiment evidence, and documentation accounting.
**Status:** Not clean; confirmed issues fixed; clean streak remains 0.

## Review Inputs

- Current implementation after Round 08 fixes.
- User constraint: skill prompts must preserve GSD/Auto capabilities and task quality, not merely run without errors.
- User constraint: explicitly audit Claude Code -> Codex conversion, including paths, hooks, MCP support assets, and multi-agent interface parameters.
- Completed read-only lanes:
  - Codex conversion conformance.
  - Prompt capability fidelity.
  - Migration/installer/assets.
  - Runtime/workstreams and research evidence.
  - Documentation/accounting.
- The lifecycle/quality-gate lane timed out and was shut down; Round 10 must re-cover that surface as a fresh lane.

## Review Lanes

| Lane | Result |
|------|--------|
| Codex conversion conformance | Confirmed stale `spawn_agent`/`send_input` parameter shapes, old `codex-reply` wording, hardcoded `wait_agent`, and a Claude model example in active Codex content. |
| Prompt capability fidelity | Confirmed official upstream Codex overlays for `auto-review-loop`, `result-to-claim`, `experiment-bridge`, and `rebuttal` were thinner than base Auto prompts and deleted important review/audit/rebuttal behavior. |
| Installer/assets | Confirmed a Claude-review override generator remained active even though the Claude-review MCP bridge itself was archived, and active MCP roots could retain generated bytecode residue. |
| Runtime/workstreams | Confirmed state-family and active-workstream pointer paths could follow symlinks; unsafe Codex session ids were lossy-normalized; primary handoff resume did not validate active workstream context. |
| Migration proof | Confirmed backup proof did not bind the root and manifest paths to `.planning/legacy-backups/<family>/<session>`. |
| Experiment evidence and claims | Confirmed root Auto `EXPERIMENT_AUDIT` handoff was not recognized, audit severity was flattened, supported claim gates could self-satisfy, and `CLAIMS` freshness needed to mark result-to-claim stale. |
| Context files | Confirmed project/rebuttal context collectors could still include directories as files. |
| Docs/accounting | Confirmed status docs needed to supersede earlier Round 06/08 accounting and record that Round 10/11 are now required. |

## Confirmed Findings

| ID | Severity | Finding | Confirmation | Fix |
|----|----------|---------|--------------|-----|
| BUG-059 | P2 | Active install exposed `generate_codex_claude_review_overrides.py` even after Claude-review MCP was reference-only. | Preview assets showed the tool under active `tools/`. | Filtered it out of active tools and preserved it under `ljx-gsd/upstream-auto/tools/`. |
| BUG-060 | P1/P2 | Several official Codex overlays deleted base Auto prompt capability. | Comparison against base Auto prompts confirmed missing hard/nightmare review, experiment integrity, pre-deploy code review, and rebuttal workspace detail. | Added source opt-outs so richer base prompts are Codex-converted for those skills. |
| BUG-061 | P2 | Active prompts retained stale Codex/Claude interface parameter shapes. | Grep and subagent review found stale `prompt`, `model_reasoning_effort`, `send_input id/agent_id`, `codex-reply`, `wait_agent`, and Claude model examples. | Added block-level Codex schema rewriting and prose normalization. |
| BUG-062 | P3 | Active MCP roots could keep unmanifested `__pycache__` bytecode. | Preview contained `.pyc` residue under active MCP server roots. | Added safe bytecode-only `__pycache__` cleanup. |
| BUG-063 | P1 | Runtime state family paths could escape through symlinked state directories. | `fs.statSync()` followed symlinks during state-family validation. | Switched to `lstat`, rejected symlinks, and validated read/list/write through the same state-family gate. |
| BUG-064 | P1/P3 | Active workstream/session pointers could escape or collide. | Legacy active pointer read/write followed symlinks, and unsafe `CODEX_SESSION_ID` values were normalized instead of rejected. | Rejected symlinked active pointers and unsafe session ids. |
| BUG-065 | P2 | Primary handoff resume did not validate the active workstream, and directory `HANDOFF.json` was treated as absent. | `resumeFromHandoff()` skipped primary workstream checks and handoff reads used existence-style checks. | Required active workstream match for primary too and added regular-file handoff validation. |
| BUG-066 | P2 | `pause-work` prompt misreported `already_paused`. | Helper returned `reasonCode: already_paused`, but generated instructions treated all `created=false` as damaged state. | Branched generated instructions on `already_paused`. |
| BUG-067 | P2 | Migration backup proof was not rooted to the canonical backup location. | A copied backup tree under a different in-project root could satisfy prior containment/provenance checks. | Required exact expected backup root, original root, manifest path, report path, and preserved original root. |
| BUG-068 | P2 | Context file collectors could include directories. | Root context used `existsSync()` and rebuttal context filtered by suffix only. | Required regular files for root and rebuttal phase context. |
| BUG-069 | P2 | Auto `$experiment-audit` handoff did not satisfy ljx audit readiness and audit severity was flattened. | ljx only checked phase-local audit markdown and returned `audited` for any present artifact. | Added root Auto audit candidates and parsed `integrity_status: pass|warn|fail`. |
| BUG-070 | P2 | Supported claim gates could self-satisfy without real evidence. | `RESULT_TO_CLAIM`/`CLAIMS` artifacts counted as support evidence, and claim-gate lacked the supported-evidence gate. | Count only results/review evidence as support, share the gate across writers, and block failed-audit supported claims. |
| BUG-071 | P2 | `CLAIMS` updates did not mark prior result-to-claim judgments stale. | Second-pass review found the Round 08 removal of `claims` from staleness inputs was too permissive. | Reintroduced `claims` into `staleRelativeTo` and marked BUG-058 superseded. |

## Verification After Fixes

Commands run after Round 09 fixes:

- `node --check bin/lib/codex-conversion.cjs`
- `node --check bin/lib/build-skills.cjs`
- `node --check bin/lib/ljx-runtime-state.cjs`
- `node --check bin/lib/ljx-state-tools.cjs`
- `node --check bin/lib/ljx-experiment-evidence-tools.cjs && node --check bin/lib/ljx-result-to-claim-tools.cjs && node --check bin/lib/ljx-claim-gate-tools.cjs`
- `node --test tests/skill-build.test.cjs`
- `node --test tests/runtime-state.test.cjs tests/runtime-core.test.cjs tests/runtime-shell.test.cjs`
- `node --test tests/experiment-evidence-tools.test.cjs tests/result-to-claim-bridge.test.cjs tests/claim-gate-bridge.test.cjs`
- `node --test tests/migration-cutover.test.cjs`
- `node bin/install.js --preview`
- Active preview grep over `.build/codex-preview/skills` and `.build/codex-preview/templates` for stale Codex MCP/Claude strings returned no matches.
- Active preview `find .build/codex-preview/mcp-servers -name '__pycache__' -o -name '*.pyc'` returned no files.
- `node --test tests/*.test.cjs` -> 622/622 pass, 39 suites.

## Clean-Round Accounting

Round 09 is not clean because it confirmed and fixed issues. The clean streak remains 0.

Next required actions:

- Superseded by Round 10 and Round 11: both later found confirmed issues.
- Because the cap is 11 rounds, final strict success was not achieved in this bounded loop.
- Current final accounting is in `ROUND-11-FINAL-REVIEW.md`, `REVIEW-LOOP-STATE.md`, and `FINAL-VERIFICATION-REPORT.md`.
