---
phase: "10-complete-discovery-and-refinement-workflows"
plan: "02"
type: "execute"
wave: 2
depends_on:
  - "10-01"
files_modified:
  - "bin/lib/ljx-research-refine-tools.cjs"
  - "bin/lib/codex-conversion.cjs"
  - "bin/lib/build-skills.cjs"
  - "bin/lib/manifest.cjs"
  - "tests/research-refine-bridge.test.cjs"
  - "tests/skill-build.test.cjs"
autonomous: true
requirements:
  - "IMPL-04"
user_setup: []
must_haves:
  truths:
    - "`ljx-GSD-research-refine` becomes a true typed `refine` workflow that restores canonical `FINAL_PROPOSAL` semantics while keeping a compatibility `METHOD_SPEC` mirror."
    - "Refine preserves bounded review-revise semantics and honest downstream handoff to `experiment-plan` without widening to later phase families."
    - "Repo-local helper behavior and installed skill wording agree on canonical refine outputs and preserved Auto fallback semantics."
  artifacts:
    - "`bin/lib/ljx-research-refine-tools.cjs` exposes canonical final-proposal targeting, compatibility mirror targeting, and optional refinement-session state targeting."
    - "`tests/research-refine-bridge.test.cjs` proves canonical refine artifact paths and honest-stop behavior."
    - "`tests/skill-build.test.cjs` proves installed refine skill wording matches the canonical-plus-compatibility contract."
  key_links:
    - "`codex-conversion.cjs` and `build-skills.cjs` keep refine's public wording aligned with the canonical proposal/report plus compatibility-mirror contract."
    - "`manifest.cjs` no longer describes refine as merely a method-spec bridge after Phase 10 work lands."
---

<objective>
Complete `ljx-GSD-research-refine` as a typed refine-phase workflow that restores canonical proposal semantics, preserves bounded iterative refinement value, and keeps downstream handoff aligned with `experiment-plan`.

Purpose: reverse bridge-era artifact drift without reintroducing a second refine control plane.
Output: richer typed refine helper behavior, aligned generated skill wording, and regression coverage for repo-local plus installed refine semantics.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-complete-discovery-and-refinement-workflows/10-CONTEXT.md
@.planning/phases/10-complete-discovery-and-refinement-workflows/10-RESEARCH.md
@LJX-GSD-CORE-COMMAND-SPECS.md
@LJX-GSD-INTERFACES.md
@LJX-GSD-SKILL-MIGRATION-DETAILED.md
@bin/lib/ljx-research-refine-tools.cjs
@bin/lib/codex-conversion.cjs
@bin/lib/build-skills.cjs
@bin/lib/manifest.cjs
@/Users/lijiaxin/.codex/skills/research-refine/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore canonical refine artifact semantics in the typed helper</name>
  <files>bin/lib/ljx-research-refine-tools.cjs</files>
  <read_first>bin/lib/ljx-phase-context.cjs, .planning/phases/10-complete-discovery-and-refinement-workflows/10-RESEARCH.md, /Users/lijiaxin/.codex/skills/research-refine/SKILL.md</read_first>
  <action>Extend `bin/lib/ljx-research-refine-tools.cjs` from a bridge helper into a real typed refine context provider. It should target canonical `{phase}-FINAL_PROPOSAL.md`, supporting `{phase}-REFINEMENT_REPORT.md`, and a compatibility `{phase}-METHOD_SPEC.md` mirror. Add optional `.planning/state/research/refinement-sessions/{phase}.json` targeting for runs that need persistent review/revise history. Preserve bounded refine semantics, explicit problem-anchor and contribution-sharpening focus, and the accepted downstream handoff to `experiment-plan` only.</action>
  <verify>node --test tests/research-refine-bridge.test.cjs</verify>
  <acceptance_criteria>
    - The helper exposes canonical final-proposal semantics instead of a method-spec-only contract.
    - Refine can target accepted structured refinement-session state when the run genuinely needs it.
    - Downstream routing stays bounded to `experiment-plan` and does not widen to later phase families.
  </acceptance_criteria>
  <done>`ljx-GSD-research-refine` now has a true typed refine helper contract with canonical proposal semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Align generated refine skill wording and manifest semantics</name>
  <files>bin/lib/codex-conversion.cjs, bin/lib/build-skills.cjs, bin/lib/manifest.cjs</files>
  <read_first>bin/lib/ljx-research-refine-tools.cjs, bin/lib/codex-conversion.cjs, bin/lib/build-skills.cjs, tests/skill-build.test.cjs</read_first>
  <action>Update generated `ljx-GSD-research-refine` skill wording so the installed skill now describes the canonical final-proposal/report outputs, the compatibility method-spec mirror, bounded iterative-refine semantics, and optional refinement-session continuity where appropriate. Keep the accepted Auto fallback wording intact and keep the downstream handoff aligned with `experiment-plan`. Update manifest rationale so it no longer describes refine as only a method-spec bridge.</action>
  <verify>node bin/install.js --preview</verify>
  <acceptance_criteria>
    - Repo-local helper, generated refine skill wording, and manifest rationale agree on the Phase 10 contract.
    - Installed skill text preserves Auto fallback semantics instead of deleting them.
    - Preview/install completes with the updated refine surface.
  </acceptance_criteria>
  <done>The installed/public refine surface matches the richer Phase 10 semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Lock canonical refine semantics with regression tests</name>
  <files>tests/research-refine-bridge.test.cjs, tests/skill-build.test.cjs</files>
  <read_first>bin/lib/ljx-research-refine-tools.cjs, tests/research-refine-bridge.test.cjs, tests/skill-build.test.cjs</read_first>
  <action>Add or update regression coverage for canonical final-proposal targeting, compatibility method-spec mirroring, optional refinement-session targeting, bounded downstream handoff, and installed-surface wording. Ensure tests fail if refine regresses to method-spec-only semantics or widens its handoff beyond `experiment-plan`.</action>
  <verify>node --test tests/research-refine-bridge.test.cjs tests/skill-build.test.cjs</verify>
  <acceptance_criteria>
    - Tests fail if typed refine context loses canonical final-proposal targeting.
    - Tests fail if installed skill wording drifts from helper behavior.
    - Tests fail if refine widens its downstream handoff beyond `experiment-plan`.
  </acceptance_criteria>
  <done>Typed refine semantics are protected by helper-level and installed-surface regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/research-refine-bridge.test.cjs tests/skill-build.test.cjs`
- [ ] `node bin/install.js --preview`
- [ ] `npm test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `ljx-GSD-research-refine` becomes a typed refine workflow with canonical proposal semantics and bounded handoff
</success_criteria>

<output>
After completion, create `.planning/phases/10-complete-discovery-and-refinement-workflows/10-02-SUMMARY.md`
</output>
