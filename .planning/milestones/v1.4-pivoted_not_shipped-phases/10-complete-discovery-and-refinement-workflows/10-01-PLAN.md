---
phase: "10-complete-discovery-and-refinement-workflows"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "bin/lib/ljx-idea-discovery-tools.cjs"
  - "bin/lib/codex-conversion.cjs"
  - "bin/lib/build-skills.cjs"
  - "bin/lib/manifest.cjs"
  - "tests/idea-discovery-bridge.test.cjs"
  - "tests/skill-build.test.cjs"
autonomous: true
requirements:
  - "IMPL-04"
user_setup: []
must_haves:
  truths:
    - "`ljx-GSD-idea-discovery` becomes a true typed `discovery` workflow instead of a narrow bridge-only report wrapper."
    - "Discovery preserves Workflow 1 core stages and ranked shortlist semantics while still stopping honestly at the discovery boundary."
    - "Repo-local helpers, generated skills, and preview/install output agree on the deeper discovery contract."
  artifacts:
    - "`bin/lib/ljx-idea-discovery-tools.cjs` exposes deeper typed discovery context, canonical phase-local outputs, and optional portfolio-state targeting."
    - "`tests/idea-discovery-bridge.test.cjs` proves typed discovery context, optional portfolio state, and honest-stop behavior."
    - "`tests/skill-build.test.cjs` proves installed skill wording matches the deeper discovery semantics."
  key_links:
    - "`codex-conversion.cjs` and `build-skills.cjs` describe the same stage boundaries and downstream handoffs as the repo-local helper."
    - "`manifest.cjs` no longer describes `idea-discovery` as only a thin report bridge after Phase 10 work lands."
---

<objective>
Complete `ljx-GSD-idea-discovery` as a typed discovery-phase workflow that preserves Workflow 1's core discovery semantics without silently expanding into the downstream refine or experiment chain.

Purpose: deepen discovery semantics while staying inside the accepted phase boundary.
Output: richer typed discovery helper behavior, aligned generated skill wording, and regression coverage for repo-local plus installed discovery semantics.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-complete-discovery-and-refinement-workflows/10-CONTEXT.md
@.planning/phases/10-complete-discovery-and-refinement-workflows/10-RESEARCH.md
@LJX-GSD-CORE-COMMAND-SPECS.md
@LJX-GSD-INTERFACES.md
@LJX-GSD-SKILL-MIGRATION-DETAILED.md
@bin/lib/ljx-idea-discovery-tools.cjs
@bin/lib/codex-conversion.cjs
@bin/lib/build-skills.cjs
@bin/lib/manifest.cjs
@/Users/lijiaxin/.codex/skills/idea-discovery/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deepen the typed discovery helper contract</name>
  <files>bin/lib/ljx-idea-discovery-tools.cjs</files>
  <read_first>bin/lib/ljx-phase-context.cjs, .planning/phases/10-complete-discovery-and-refinement-workflows/10-RESEARCH.md, /Users/lijiaxin/.codex/skills/idea-discovery/SKILL.md</read_first>
  <action>Extend `bin/lib/ljx-idea-discovery-tools.cjs` from a thin bridge helper into a real typed discovery context provider. Keep phase-local outputs and the accepted boundary, but add the richer semantics Phase 10 expects: canonical discovery workflow metadata, explicit ranked-shortlist behavior, optional portfolio-state targeting under `.planning/state/research/idea-portfolios/{phase}.json`, and explicit downstream handoffs that stop at `research-refine`/manual review tools instead of widening to experiments. Preserve honest-stop behavior for missing planning, missing phase, malformed config, and unsupported phase types.</action>
  <verify>node --test tests/idea-discovery-bridge.test.cjs</verify>
  <acceptance_criteria>
    - The helper exposes a typed discovery workflow contract richer than "write one report".
    - Discovery can target accepted structured portfolio state when the run needs persistent shortlist memory.
    - Downstream routing remains explicit and bounded at the discovery boundary.
  </acceptance_criteria>
  <done>`ljx-GSD-idea-discovery` now has a true typed discovery helper contract.</done>
</task>

<task type="auto">
  <name>Task 2: Align generated discovery skill wording and manifest semantics</name>
  <files>bin/lib/codex-conversion.cjs, bin/lib/build-skills.cjs, bin/lib/manifest.cjs</files>
  <read_first>bin/lib/ljx-idea-discovery-tools.cjs, bin/lib/codex-conversion.cjs, bin/lib/build-skills.cjs, tests/skill-build.test.cjs</read_first>
  <action>Update generated `ljx-GSD-idea-discovery` skill wording so installed skills describe the same deeper typed discovery semantics as the repo-local helper. Keep the accepted Workflow 1 core-stage wording, keep `research-refine` as the downstream boundary, and mention optional portfolio-state continuation only when the helper exposes it. Adjust the manifest rationale so it no longer describes discovery as only a thin report wrapper once the deeper semantics exist.</action>
  <verify>node bin/install.js --preview</verify>
  <acceptance_criteria>
    - Repo-local helper, generated skill wording, and manifest rationale agree on what discovery now does.
    - Installed skill text still preserves Auto fallback semantics instead of deleting them.
    - Preview/install completes with the updated discovery surface.
  </acceptance_criteria>
  <done>The installed/public discovery surface reflects the real Phase 10 semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Lock typed discovery semantics with regression tests</name>
  <files>tests/idea-discovery-bridge.test.cjs, tests/skill-build.test.cjs</files>
  <read_first>bin/lib/ljx-idea-discovery-tools.cjs, tests/idea-discovery-bridge.test.cjs, tests/skill-build.test.cjs</read_first>
  <action>Add or update regression coverage for richer discovery context, optional portfolio-state targeting, bounded downstream handoff semantics, and installed-surface wording. Keep tests focused on discovery semantics rather than later refine/experiment state. Ensure they fail if discovery regresses to a report-only contract or widens its downstream boundary.</action>
  <verify>node --test tests/idea-discovery-bridge.test.cjs tests/skill-build.test.cjs</verify>
  <acceptance_criteria>
    - Tests fail if typed discovery context loses the deeper stage or shortlist semantics.
    - Tests fail if installed skill wording drifts from helper behavior.
    - Tests fail if discovery widens beyond the accepted refine boundary.
  </acceptance_criteria>
  <done>Typed discovery semantics are protected by helper-level and installed-surface regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/idea-discovery-bridge.test.cjs tests/skill-build.test.cjs`
- [ ] `node bin/install.js --preview`
- [ ] `npm test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `ljx-GSD-idea-discovery` becomes a typed discovery workflow with explicit state and bounded handoff semantics
</success_criteria>

<output>
After completion, create `.planning/phases/10-complete-discovery-and-refinement-workflows/10-01-SUMMARY.md`
</output>
