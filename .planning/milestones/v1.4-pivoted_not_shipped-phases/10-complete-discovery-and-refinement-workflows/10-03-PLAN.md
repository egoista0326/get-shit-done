---
phase: "10-complete-discovery-and-refinement-workflows"
plan: "03"
type: "execute"
wave: 3
depends_on:
  - "10-01"
  - "10-02"
files_modified:
  - "bin/lib/ljx-runtime-state.cjs"
  - "bin/lib/ljx-lifecycle-shell-tools.cjs"
  - "bin/lib/ljx-state-tools.cjs"
  - "tests/plan-phase-shell.test.cjs"
  - "tests/execute-phase-shell.test.cjs"
  - "tests/lifecycle-next.test.cjs"
  - "tests/lifecycle-state-sync.test.cjs"
autonomous: true
requirements:
  - "IMPL-04"
user_setup: []
must_haves:
  truths:
    - "Accepted discovery/refine structured runtime state families exist and are used only when the workflow genuinely needs persistent iteration state."
    - "Lifecycle adoption and `next` routing understand the richer Phase 10 discovery/refine artifact set instead of only the thinner Phase 08 bridge artifacts."
    - "Discovery/refine handoff semantics remain explicit and honest after the deeper state and lifecycle integration lands."
  artifacts:
    - "`bin/lib/ljx-runtime-state.cjs` supports the accepted research state families under `.planning/state/research/`."
    - "`bin/lib/ljx-lifecycle-shell-tools.cjs` and `bin/lib/ljx-state-tools.cjs` consume the richer discovery/refine artifact and state contracts."
    - "`tests/plan-phase-shell.test.cjs`, `tests/execute-phase-shell.test.cjs`, `tests/lifecycle-next.test.cjs`, and `tests/lifecycle-state-sync.test.cjs` prove lifecycle adoption, recommendation, and state-sync correctness."
  key_links:
    - "Phase 10 should finish by converging helper semantics, lifecycle adoption, and research-state families onto one shared rule set."
    - "The final cross-cutting slice should encode the stricter semantic-reuse discipline learned in the Phase 07-09 retrospective."
---

<objective>
Finish Phase 10 by landing the accepted discovery/refine research-state families and by making lifecycle adoption plus recommendation logic consume the richer discovery/refine contract coherently.

Purpose: ensure the deeper Phase 10 domain engines are actually first-class lifecycle citizens rather than isolated helper upgrades.
Output: accepted research-state family support, lifecycle adoption updates, and regression coverage proving discovery/refine completion is real across shell and routing behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-complete-discovery-and-refinement-workflows/10-CONTEXT.md
@.planning/phases/10-complete-discovery-and-refinement-workflows/10-RESEARCH.md
@.planning/phases/05-migration-and-parallelism-strategy/05-02-PLAN.md
@bin/lib/ljx-runtime-state.cjs
@bin/lib/ljx-lifecycle-shell-tools.cjs
@bin/lib/ljx-state-tools.cjs
@bin/lib/ljx-idea-discovery-tools.cjs
@bin/lib/ljx-research-refine-tools.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land accepted research-state family support in the runtime layer</name>
  <files>bin/lib/ljx-runtime-state.cjs</files>
  <read_first>.planning/phases/05-migration-and-parallelism-strategy/05-02-PLAN.md, bin/lib/ljx-runtime-state.cjs, .planning/phases/10-complete-discovery-and-refinement-workflows/10-RESEARCH.md</read_first>
  <action>Extend `bin/lib/ljx-runtime-state.cjs` so the accepted research state families for Phase 10 are real runtime citizens: `.planning/state/research/idea-portfolios/{phase}.json` and `.planning/state/research/refinement-sessions/{phase}.json`. Keep the API consistent with the existing runtime-state helpers and preserve honest failure behavior when `.planning/` cannot be resolved. Do not make the runtime eagerly create empty research-state records by default.</action>
  <verify>node --test tests/lifecycle-state-sync.test.cjs</verify>
  <acceptance_criteria>
    - The runtime layer can read and write the accepted Phase 10 research families without ad hoc command-local filesystem code.
    - Research families live at the accepted Phase 5 paths, not new improvised directories.
    - Existing `phase-records`, `sessions`, and `workstreams` behavior remains intact.
  </acceptance_criteria>
  <done>The shared runtime supports the accepted discovery/refine research-state families.</done>
</task>

<task type="auto">
  <name>Task 2: Update lifecycle adoption and recommendation logic for richer discovery/refine semantics</name>
  <files>bin/lib/ljx-lifecycle-shell-tools.cjs, bin/lib/ljx-state-tools.cjs</files>
  <read_first>bin/lib/ljx-runtime-state.cjs, bin/lib/ljx-lifecycle-shell-tools.cjs, bin/lib/ljx-state-tools.cjs, tests/plan-phase-shell.test.cjs, tests/execute-phase-shell.test.cjs</read_first>
  <action>Broaden lifecycle adoption and routing so the shell and recommendation path understand the deeper discovery/refine artifact set. Discovery adoption should tolerate richer portfolio outputs; refine adoption should recognize canonical `FINAL_PROPOSAL.md`, supporting `REFINEMENT_REPORT.md`, and compatibility `METHOD_SPEC.md` without forcing reruns. Keep downstream recommendations explicit: discovery remains bounded to refine/manual review tools, refine remains bounded to `experiment-plan`.</action>
  <verify>node --test tests/plan-phase-shell.test.cjs tests/execute-phase-shell.test.cjs tests/lifecycle-next.test.cjs tests/lifecycle-state-sync.test.cjs</verify>
  <acceptance_criteria>
    - Lifecycle adoption no longer assumes the old thin discovery/refine artifact set is the only valid output.
    - `next` and related routing remain phase-chain-correct after the richer Phase 10 outputs land.
    - The shell records adoption provenance and evidence artifacts for the richer discovery/refine contract.
  </acceptance_criteria>
  <done>Lifecycle adoption and recommendation logic now treat discovery/refine as fully phase-owned workflows.</done>
</task>

<task type="auto">
  <name>Task 3: Add cross-surface regression coverage for state and handoff semantics</name>
  <files>tests/plan-phase-shell.test.cjs, tests/execute-phase-shell.test.cjs, tests/lifecycle-next.test.cjs, tests/lifecycle-state-sync.test.cjs</files>
  <read_first>bin/lib/ljx-runtime-state.cjs, bin/lib/ljx-lifecycle-shell-tools.cjs, tests/plan-phase-shell.test.cjs, tests/execute-phase-shell.test.cjs, tests/lifecycle-next.test.cjs, tests/lifecycle-state-sync.test.cjs</read_first>
  <action>Add or update regression tests that fail if: accepted research-state families cannot be read/written, lifecycle adoption ignores canonical Phase 10 refine artifacts, discovery/refine continuation widens beyond the accepted handoff boundary, or lifecycle state sync loses adoption provenance after the richer artifacts land. Keep tests at the shell/runtime boundary so later phases can build on them instead of reopening the same contract.</action>
  <verify>node --test tests/plan-phase-shell.test.cjs tests/execute-phase-shell.test.cjs tests/lifecycle-next.test.cjs tests/lifecycle-state-sync.test.cjs</verify>
  <acceptance_criteria>
    - Tests fail if research-state family support regresses.
    - Tests fail if lifecycle adoption regresses to the thinner bridge-only artifact contract.
    - Tests fail if `next` starts implying the wrong downstream continuation after discovery or refine.
  </acceptance_criteria>
  <done>Phase 10's cross-cutting state and handoff semantics are protected by regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/plan-phase-shell.test.cjs tests/execute-phase-shell.test.cjs tests/lifecycle-next.test.cjs tests/lifecycle-state-sync.test.cjs`
- [ ] `node bin/install.js --preview`
- [ ] `npm test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Discovery/refine runtime state, lifecycle adoption, and handoff semantics all converge on the richer Phase 10 contract
</success_criteria>

<output>
After completion, create `.planning/phases/10-complete-discovery-and-refinement-workflows/10-03-SUMMARY.md`
</output>
