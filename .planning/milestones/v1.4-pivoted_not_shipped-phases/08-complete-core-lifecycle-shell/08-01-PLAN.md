---
phase: "08-complete-core-lifecycle-shell"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "bin/lib/ljx-lifecycle-shell-tools.cjs"
  - "bin/lib/codex-conversion.cjs"
  - "bin/lib/build-skills.cjs"
  - "bin/lib/manifest.cjs"
  - "tests/discuss-phase-shell.test.cjs"
autonomous: true
requirements:
  - "IMPL-02"
user_setup: []
must_haves:
  truths:
    - "Typed discuss-phase context is resolved from the authoritative runtime substrate plus one deterministic lifecycle route table rather than bridge-era placeholder heuristics."
    - "Focused discuss behavior remains the public shell contract, including suggested defaults and a small number of high-value live questions."
    - "The preview/install surface can expose `ljx-GSD-discuss-phase` without pretending that later lifecycle commands are already bridge-ready."
  artifacts:
    - "bin/lib/ljx-lifecycle-shell-tools.cjs provides shared lifecycle route resolution and focused discuss-phase context helpers."
    - "tests/discuss-phase-shell.test.cjs proves typed discuss context, route defaults, and honest-stop behavior."
  key_links:
    - "codex-conversion.cjs and build-skills.cjs generate an installed `ljx-GSD-discuss-phase` skill backed by the new lifecycle-shell helper."
    - "manifest.cjs reflects `ljx-GSD-discuss-phase` as bridge-ready only after the helper and builder paths exist."
---

<objective>
Implement the first real typed lifecycle entrypoint by landing a focused `ljx-GSD-discuss-phase` on top of one shared lifecycle-shell helper.

Purpose: convert the accepted Phase 08 context decisions into a real discuss shell that reuses GSD's outer interaction model while reading authoritative ljx-GSD runtime truth.
Output: a shared lifecycle-shell helper, a custom discuss-phase skill builder, and regression coverage proving focused typed discuss behavior is bridge-ready.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-complete-core-lifecycle-shell/08-CONTEXT.md
@.planning/phases/08-complete-core-lifecycle-shell/08-RESEARCH.md
@LJX-GSD-CORE-COMMAND-SPECS.md
@LJX-GSD-INTERFACES.md
@bin/lib/ljx-runtime-core.cjs
@bin/lib/ljx-runtime-state.cjs
@bin/lib/ljx-phase-context.cjs
@bin/lib/codex-conversion.cjs
@bin/lib/build-skills.cjs
@/Users/lijiaxin/.codex/get-shit-done/workflows/discuss-phase.md
@/Users/lijiaxin/.codex/get-shit-done/bin/lib/init.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create one shared lifecycle-shell helper with focused discuss context</name>
  <files>bin/lib/ljx-lifecycle-shell-tools.cjs</files>
  <read_first>bin/lib/ljx-runtime-core.cjs, bin/lib/ljx-runtime-state.cjs, bin/lib/ljx-phase-context.cjs, .planning/phases/08-complete-core-lifecycle-shell/08-RESEARCH.md</read_first>
  <action>Create `bin/lib/ljx-lifecycle-shell-tools.cjs` as the shared shell layer above the Phase 07 substrate. It should resolve one default primary lifecycle route per `phase_type`, keep advanced direct tools visible, and allow a future phase-record lifecycle-route override without requiring a new phase taxonomy now. Add focused discuss-phase context helpers that return: target phase, phase type, authoritative config view, current context/research artifacts, primary question, recommended gray-area categories, default lifecycle route, advanced direct tools, and honest-stop metadata. Keep the shell deterministic: `analysis` should default to `claim-gate`, `paper` to `paper-pipeline`, and `experiment` should distinguish planning route versus execution route. Do not implement planning or execution transitions yet beyond the minimal lifecycle metadata needed for discuss-phase.</action>
  <verify>node --test tests/discuss-phase-shell.test.cjs</verify>
  <acceptance_criteria>
    - One shared helper now resolves typed lifecycle discuss context and primary route defaults.
    - Focused discuss context is built from authoritative runtime state, not placeholder file-count inference.
    - The helper exposes honest-stop results for missing planning, malformed config, or phase-resolution conflicts.
  </acceptance_criteria>
  <done>A reusable lifecycle-shell helper exists and can supply focused typed discuss context safely.</done>
</task>

<task type="auto">
  <name>Task 2: Add a custom `ljx-GSD-discuss-phase` builder and promote it to the preview surface</name>
  <files>bin/lib/codex-conversion.cjs, bin/lib/build-skills.cjs, bin/lib/manifest.cjs</files>
  <read_first>bin/lib/ljx-lifecycle-shell-tools.cjs, bin/lib/codex-conversion.cjs, bin/lib/build-skills.cjs, /Users/lijiaxin/.codex/get-shit-done/workflows/discuss-phase.md</read_first>
  <action>Add a dedicated `buildDiscussPhaseSkill` path in `codex-conversion.cjs` and wire it into `build-skills.cjs`. The generated skill must call the lifecycle-shell helper first, preserve the accepted focused-discussion style, and stop honestly on missing planning or phase/config conflicts instead of trying to run stock GSD directly. Update `manifest.cjs` so `ljx-GSD-discuss-phase` becomes bridge-ready only once this builder path exists. Ensure preview/runtime install copies the new lifecycle-shell helper into the installed runtime tree.</action>
  <verify>node bin/install.js --preview</verify>
  <acceptance_criteria>
    - The preview/install tree includes a generated `ljx-GSD-discuss-phase` skill plus the shared lifecycle helper it depends on.
    - The skill preserves focused discuss semantics rather than reverting to a broad stock GSD questionnaire.
    - Manifest readiness and generated skill availability stay aligned.
  </acceptance_criteria>
  <done>`ljx-GSD-discuss-phase` is a real bridge-ready public command on the preview surface.</done>
</task>

<task type="auto">
  <name>Task 3: Lock focused discuss behavior with lifecycle-shell regression tests</name>
  <files>tests/discuss-phase-shell.test.cjs</files>
  <read_first>bin/lib/ljx-lifecycle-shell-tools.cjs, tests/runtime-shell.test.cjs, .planning/phases/08-complete-core-lifecycle-shell/08-CONTEXT.md</read_first>
  <action>Add focused `node:test` coverage for: typed phase-route defaults, analysis defaulting to `claim-gate`, paper defaulting to `paper-pipeline`, honest-stop behavior on malformed config and phase conflicts, preservation of advanced direct tools, and preview-surface readiness assumptions for discuss-phase. Keep tests at the helper/build boundary; they should validate Phase 08 shell behavior, not later full planning or execution semantics.</action>
  <verify>node --test tests/discuss-phase-shell.test.cjs && node bin/install.js --preview</verify>
  <acceptance_criteria>
    - Tests fail if discuss-phase loses the focused typed-shell contract.
    - Tests fail if the route table drifts from accepted defaults without an explicit override.
    - Tests fail if preview build plumbing omits the discuss-phase runtime dependency chain.
  </acceptance_criteria>
  <done>The first lifecycle entrypoint is protected by helper-level and preview-surface regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/discuss-phase-shell.test.cjs`
- [ ] `node bin/install.js --preview`
- [ ] `npm test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `ljx-GSD-discuss-phase` becomes a focused typed lifecycle entrypoint backed by the authoritative runtime substrate
</success_criteria>

<output>
After completion, create `.planning/phases/08-complete-core-lifecycle-shell/08-01-SUMMARY.md`
</output>
