---
phase: "08-complete-core-lifecycle-shell"
plan: "03"
type: "execute"
wave: 3
depends_on:
  - "08-01"
  - "08-02"
files_modified:
  - "bin/lib/ljx-lifecycle-shell-tools.cjs"
  - "bin/lib/codex-conversion.cjs"
  - "bin/lib/build-skills.cjs"
  - "bin/lib/manifest.cjs"
  - "tests/execute-phase-shell.test.cjs"
autonomous: true
requirements:
  - "IMPL-02"
user_setup: []
must_haves:
  truths:
    - "Execute-phase becomes typed and authoritative: engineering phases can execute real plan inventories, while research-native phases dispatch to the accepted bounded direct workflows instead of bridge-era placeholders."
    - "Execution can adopt existing valid outputs into lifecycle summary state honestly rather than rerunning a direct workflow by default."
    - "Execute-phase keeps Phase 08 boundaries intact by routing toward existing quality gates without swallowing Phase 09 freshness/fix semantics."
  artifacts:
    - "bin/lib/ljx-lifecycle-shell-tools.cjs provides typed execute-phase routing, execution-adoption checks, and summary-wrapper metadata."
    - "tests/execute-phase-shell.test.cjs proves typed execute routing, adoption behavior, and honest-stop boundaries."
  key_links:
    - "codex-conversion.cjs and build-skills.cjs generate an installed `ljx-GSD-execute-phase` skill backed by the lifecycle-shell helper."
    - "manifest.cjs reflects `ljx-GSD-execute-phase` as bridge-ready only after the execute shell and build surface both exist."
---

<objective>
Implement a typed `ljx-GSD-execute-phase` that can either execute engineering plans or route bounded research-native phases through their accepted direct workflows without fake lifecycle routing.

Purpose: make execution a real lifecycle step across typed phases while preserving the current bridge-ready research helpers and the later Phase 09 quality-gate boundary.
Output: shared execute-phase routing/adoption logic, a custom execute-phase skill builder, and regression coverage proving typed execution works honestly.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-complete-core-lifecycle-shell/08-CONTEXT.md
@.planning/phases/08-complete-core-lifecycle-shell/08-RESEARCH.md
@.planning/phases/08-complete-core-lifecycle-shell/08-01-PLAN.md
@.planning/phases/08-complete-core-lifecycle-shell/08-02-PLAN.md
@LJX-GSD-CORE-COMMAND-SPECS.md
@LJX-GSD-INTERFACES.md
@bin/lib/ljx-lifecycle-shell-tools.cjs
@bin/lib/codex-conversion.cjs
@bin/lib/build-skills.cjs
@bin/lib/manifest.cjs
@/Users/lijiaxin/.codex/get-shit-done/workflows/execute-phase.md
@/Users/lijiaxin/.codex/skills/idea-discovery/SKILL.md
@/Users/lijiaxin/.codex/skills/research-refine/SKILL.md
@/Users/lijiaxin/.codex/skills/experiment-plan/SKILL.md
@/Users/lijiaxin/.codex/skills/experiment-bridge/SKILL.md
@/Users/lijiaxin/.codex/skills/paper-writing/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement typed execute-phase routing and execution-adoption semantics</name>
  <files>bin/lib/ljx-lifecycle-shell-tools.cjs</files>
  <read_first>bin/lib/ljx-lifecycle-shell-tools.cjs, .planning/phases/08-complete-core-lifecycle-shell/08-RESEARCH.md, LJX-GSD-CORE-COMMAND-SPECS.md, /Users/lijiaxin/.codex/get-shit-done/workflows/execute-phase.md</read_first>
  <action>Extend the shared lifecycle-shell helper so `execute-phase` can resolve a typed execution route for every accepted phase type. Engineering phases should keep the GSD-style incomplete-plan execution path. Discovery should execute through `idea-discovery`, refine through `research-refine`, experiment through `experiment-bridge`, analysis through the default `claim-gate` route, and paper through `paper-pipeline`, with advanced direct tools still visible. Add execution-adoption checks so valid existing outputs can satisfy the execute step honestly; when adopted, write or refresh thin lifecycle `SUMMARY` wrappers and authoritative execute-state fields instead of rerunning the workflow by default. Keep quality-gate integration narrow: route toward `code-review` and `verify-work` where already accepted, but do not implement Phase 09 freshness/fix-loop semantics here.</action>
  <verify>node --test tests/execute-phase-shell.test.cjs</verify>
  <acceptance_criteria>
    - Execute-phase can resolve a deterministic execution route for all accepted phase types.
    - Existing valid execution outputs can be adopted into lifecycle summary state without fake reruns.
    - Engineering execution stays GSD-like while research-native execution remains bounded to accepted direct workflows.
  </acceptance_criteria>
  <done>The lifecycle shell now knows how execution works for each typed phase and how to adopt valid outputs safely.</done>
</task>

<task type="auto">
  <name>Task 2: Add a custom `ljx-GSD-execute-phase` builder and preview/install support</name>
  <files>bin/lib/codex-conversion.cjs, bin/lib/build-skills.cjs, bin/lib/manifest.cjs</files>
  <read_first>bin/lib/ljx-lifecycle-shell-tools.cjs, bin/lib/codex-conversion.cjs, bin/lib/build-skills.cjs, /Users/lijiaxin/.codex/get-shit-done/workflows/execute-phase.md</read_first>
  <action>Add a dedicated `buildExecutePhaseSkill` path that runs the lifecycle-shell helper first and then preserves the accepted shell behavior: engineering phases may execute plan inventories with GSD-style structure, while research-native phases route to bounded primary direct workflows and produce thin lifecycle summaries. The generated skill must stay honest about what actually ran, stop plainly on damaged project state, and preserve the explicit recommendation contract after execution. Update `build-skills.cjs` and `manifest.cjs` so `ljx-GSD-execute-phase` becomes bridge-ready only when helper, builder, and runtime-copy support all exist.</action>
  <verify>node bin/install.js --preview</verify>
  <acceptance_criteria>
    - The preview/install surface includes a generated `ljx-GSD-execute-phase` skill backed by the shared lifecycle helper.
    - The skill preserves the accepted distinction between engineering execution orchestration and bounded research-native workflow dispatch.
    - Execute-phase readiness in the manifest stays aligned with actual generated surface support.
  </acceptance_criteria>
  <done>`ljx-GSD-execute-phase` becomes a real bridge-ready lifecycle command on the preview surface.</done>
</task>

<task type="auto">
  <name>Task 3: Lock typed execution behavior with regression coverage</name>
  <files>tests/execute-phase-shell.test.cjs</files>
  <read_first>bin/lib/ljx-lifecycle-shell-tools.cjs, tests/plan-phase-shell.test.cjs, .planning/phases/08-complete-core-lifecycle-shell/08-CONTEXT.md</read_first>
  <action>Add focused tests for: engineering execution routing from plan inventory, discovery/refine/paper direct execution routes, experiment planning-versus-execution separation, analysis defaulting to `claim-gate` as the primary execute route, adoption of existing execution outputs into thin lifecycle summaries, and honest-stop behavior on malformed config or unresolvable phases. Keep assertions tied to Phase 08 execution semantics and make sure they do not silently assume later Phase 09 freshness/fix-loop logic already exists.</action>
  <verify>node --test tests/execute-phase-shell.test.cjs && node bin/install.js --preview</verify>
  <acceptance_criteria>
    - Tests fail if typed execute routes drift from accepted defaults.
    - Tests fail if execution adoption silently reruns workflows or fabricates lifecycle completion.
    - Tests fail if preview build plumbing omits the execute-phase runtime dependency chain.
  </acceptance_criteria>
  <done>The typed execution shell is protected by route/adoption regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/execute-phase-shell.test.cjs`
- [ ] `node bin/install.js --preview`
- [ ] `npm test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `ljx-GSD-execute-phase` becomes a typed lifecycle execution entrypoint that preserves GSD orchestration shape and bounded Auto workflow semantics
</success_criteria>

<output>
After completion, create `.planning/phases/08-complete-core-lifecycle-shell/08-03-SUMMARY.md`
</output>
