---
phase: "08-complete-core-lifecycle-shell"
plan: "04"
type: "execute"
wave: 4
depends_on:
  - "08-01"
  - "08-02"
  - "08-03"
files_modified:
  - "bin/lib/ljx-lifecycle-shell-tools.cjs"
  - "bin/lib/ljx-state-tools.cjs"
  - "bin/lib/codex-conversion.cjs"
  - "bin/lib/build-skills.cjs"
  - "bin/lib/manifest.cjs"
  - "tests/lifecycle-next.test.cjs"
  - "tests/runtime-shell.test.cjs"
autonomous: true
requirements:
  - "IMPL-02"
user_setup: []
must_haves:
  truths:
    - "Next-step resolution is lifecycle-aware and structured-state-first, not a helper-safe file-count heuristic."
    - "`ljx-GSD-next` executes one safe action, then recomputes recommendations from updated authoritative state."
    - "The preview/install surface no longer treats `discuss-phase`, `plan-phase`, and `execute-phase` as deferred once their bridge-ready builders and helpers exist."
  artifacts:
    - "bin/lib/ljx-state-tools.cjs and bin/lib/ljx-lifecycle-shell-tools.cjs provide the authoritative lifecycle-aware next resolver."
    - "tests/lifecycle-next.test.cjs and tests/runtime-shell.test.cjs prove one-step progression, honest-stop behavior, and installed-surface filtering."
  key_links:
    - "codex-conversion.cjs updates the `ljx-GSD-next` skill wording from helper-safe/deferred routing to lifecycle-aware progression."
    - "manifest.cjs, build-skills.cjs, and preview install output stay aligned for the full Phase 08 command surface."
---

<objective>
Promote `ljx-GSD-next` from helper-safe routing to real lifecycle-aware progression on top of the new typed discuss/plan/execute shell.

Purpose: finish Phase 08 by making continuation trustworthy once the typed lifecycle entrypoints exist.
Output: a lifecycle-aware next resolver, updated next skill/build wording, and regression coverage proving one-step progression works without swallowing later phases.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-complete-core-lifecycle-shell/08-CONTEXT.md
@.planning/phases/08-complete-core-lifecycle-shell/08-RESEARCH.md
@.planning/phases/08-complete-core-lifecycle-shell/08-01-PLAN.md
@.planning/phases/08-complete-core-lifecycle-shell/08-02-PLAN.md
@.planning/phases/08-complete-core-lifecycle-shell/08-03-PLAN.md
@LJX-GSD-CORE-COMMAND-SPECS.md
@LJX-GSD-INTERFACES.md
@bin/lib/ljx-lifecycle-shell-tools.cjs
@bin/lib/ljx-state-tools.cjs
@bin/lib/codex-conversion.cjs
@bin/lib/build-skills.cjs
@bin/lib/manifest.cjs
@tests/runtime-shell.test.cjs
@/Users/lijiaxin/.codex/get-shit-done/bin/lib/init.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace helper-safe next heuristics with a lifecycle-aware resolver</name>
  <files>bin/lib/ljx-lifecycle-shell-tools.cjs, bin/lib/ljx-state-tools.cjs</files>
  <read_first>bin/lib/ljx-lifecycle-shell-tools.cjs, bin/lib/ljx-state-tools.cjs, .planning/phases/08-complete-core-lifecycle-shell/08-RESEARCH.md, LJX-GSD-INTERFACES.md</read_first>
  <action>Replace the current `inferRecommendations()` / helper-safe `next` path with a lifecycle-aware resolver that reads authoritative lifecycle status from phase records first, then uses thin lifecycle artifacts and accepted direct-artifact adoption as supporting evidence. Preserve pause/resume, missing-planning, malformed-config, and malformed-handoff honest-stop behavior. Keep the next contract bounded: execute only one top-ranked safe action, then recompute. Once Phase 08 entrypoints exist, `discuss-phase`, `plan-phase`, and `execute-phase` should no longer surface as deferred lifecycle commands when they are actually bridge-ready. Continue to route toward existing `code-review` and `verify-work` behavior where already accepted, but do not implement Phase 09 freshness/fix-loop semantics here.</action>
  <verify>node --test tests/lifecycle-next.test.cjs tests/runtime-shell.test.cjs</verify>
  <acceptance_criteria>
    - Next-step resolution is based on authoritative lifecycle state and typed route rules rather than only file counts.
    - `ljx-GSD-next` executes at most one safe lifecycle action per invocation, then recomputes.
    - Honest-stop behavior for damaged project state remains intact.
  </acceptance_criteria>
  <done>The next-action resolver is now lifecycle-aware and structured-state-first.</done>
</task>

<task type="auto">
  <name>Task 2: Update next-skill/build wording and align the public readiness surface</name>
  <files>bin/lib/codex-conversion.cjs, bin/lib/build-skills.cjs, bin/lib/manifest.cjs</files>
  <read_first>bin/lib/ljx-state-tools.cjs, bin/lib/codex-conversion.cjs, bin/lib/build-skills.cjs, bin/lib/manifest.cjs</read_first>
  <action>Update the generated `ljx-GSD-next` skill wording so it no longer describes itself as helper-safe-only or primarily deferred. The skill should explain one-step lifecycle-aware progression, honest-stop behavior on damaged state, and the fact that it recomputes recommendations after each safe action. Align `manifest.cjs` rationale and readiness state for `ljx-GSD-next`, `ljx-GSD-discuss-phase`, `ljx-GSD-plan-phase`, and `ljx-GSD-execute-phase`, and ensure `build-skills.cjs` plus preview install output expose the full Phase 08 lifecycle shell consistently.</action>
  <verify>node bin/install.js --preview</verify>
  <acceptance_criteria>
    - The generated `ljx-GSD-next` skill reflects lifecycle-aware progression rather than helper-safe-only routing.
    - Manifest readiness, built skills, and installed runtime output agree on the full Phase 08 lifecycle surface.
    - Installed-surface filtering in runtime recommendations no longer hides Phase 08 commands once they are genuinely bridge-ready.
  </acceptance_criteria>
  <done>The public skill surface and runtime readiness map are aligned with the new lifecycle-aware shell.</done>
</task>

<task type="auto">
  <name>Task 3: Lock one-step lifecycle progression with regression coverage</name>
  <files>tests/lifecycle-next.test.cjs, tests/runtime-shell.test.cjs</files>
  <read_first>bin/lib/ljx-state-tools.cjs, bin/lib/ljx-lifecycle-shell-tools.cjs, tests/runtime-shell.test.cjs</read_first>
  <action>Add focused regression tests for: one-step progression across discuss -> plan -> execute boundaries, structured-state-first recommendation resolution, installed-surface filtering after public promotion of Phase 08 commands, continued pause/resume priority, malformed config and malformed handoff honest-stop behavior, and the Phase 09 boundary that still routes toward quality gates without silently executing deeper freshness/fix semantics. Keep the tests explicit about recomputation after each safe action.</action>
  <verify>node --test tests/lifecycle-next.test.cjs tests/runtime-shell.test.cjs</verify>
  <acceptance_criteria>
    - Tests fail if `next` chains multiple lifecycle stages in one call.
    - Tests fail if recommendations still depend primarily on file-count heuristics when lifecycle state exists.
    - Tests fail if installed-surface filtering and manifest readiness drift apart after Phase 08 promotion.
  </acceptance_criteria>
  <done>The upgraded next resolver is protected by regression coverage and ready to anchor Phase 08 completion.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/lifecycle-next.test.cjs tests/runtime-shell.test.cjs`
- [ ] `node bin/install.js --preview`
- [ ] `npm test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `ljx-GSD-next` becomes a lifecycle-aware one-step continuation command backed by authoritative typed-shell state
</success_criteria>

<output>
After completion, create `.planning/phases/08-complete-core-lifecycle-shell/08-04-SUMMARY.md`
</output>
