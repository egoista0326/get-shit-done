---
phase: "08-complete-core-lifecycle-shell"
plan: "02"
type: "execute"
wave: 2
depends_on:
  - "08-01"
files_modified:
  - "bin/lib/ljx-lifecycle-shell-tools.cjs"
  - "bin/lib/codex-conversion.cjs"
  - "bin/lib/build-skills.cjs"
  - "bin/lib/manifest.cjs"
  - "tests/plan-phase-shell.test.cjs"
autonomous: true
requirements:
  - "IMPL-02"
user_setup: []
must_haves:
  truths:
    - "Plan-phase becomes phase-type-aware: engineering phases still produce real plan inventories, while research-native phases produce thin lifecycle plans or adopt existing direct-workflow planning artifacts."
    - "Direct-workflow adoption is explicit and honest: valid artifacts become lifecycle truth through thin wrappers and phase-record updates instead of hidden reruns."
    - "The public preview/install surface can expose `ljx-GSD-plan-phase` without reopening Phase 10-12 domain-rewrite scope."
  artifacts:
    - "bin/lib/ljx-lifecycle-shell-tools.cjs provides phase-type-aware plan context, direct-artifact adoption checks, and thin lifecycle plan-wrapper metadata."
    - "tests/plan-phase-shell.test.cjs proves plan routing, plan adoption, and honest-stop behavior across typed phases."
  key_links:
    - "codex-conversion.cjs and build-skills.cjs generate an installed `ljx-GSD-plan-phase` skill backed by the shared lifecycle-shell helper."
    - "manifest.cjs reflects `ljx-GSD-plan-phase` as bridge-ready only once planning-route and build-surface support are present."
---

<objective>
Implement a phase-type-aware `ljx-GSD-plan-phase` that keeps GSD-style lifecycle planning visible while reusing the accepted direct research workflows instead of rewriting them.

Purpose: make planning the authoritative shell entrypoint for typed phases without swallowing later domain-completion phases.
Output: shared plan-phase routing/adoption logic, a custom plan-phase skill builder, and regression coverage proving typed planning works across engineering and research-native phases.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-complete-core-lifecycle-shell/08-CONTEXT.md
@.planning/phases/08-complete-core-lifecycle-shell/08-RESEARCH.md
@.planning/phases/08-complete-core-lifecycle-shell/08-01-PLAN.md
@LJX-GSD-CORE-COMMAND-SPECS.md
@LJX-GSD-INTERFACES.md
@bin/lib/ljx-lifecycle-shell-tools.cjs
@bin/lib/ljx-phase-context.cjs
@bin/lib/codex-conversion.cjs
@bin/lib/build-skills.cjs
@/Users/lijiaxin/.codex/get-shit-done/workflows/plan-phase.md
@/Users/lijiaxin/.codex/get-shit-done/bin/lib/init.cjs
@/Users/lijiaxin/.codex/skills/idea-discovery/SKILL.md
@/Users/lijiaxin/.codex/skills/research-refine/SKILL.md
@/Users/lijiaxin/.codex/skills/experiment-plan/SKILL.md
@/Users/lijiaxin/.codex/skills/paper-writing/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement phase-type-aware planning routes and direct-artifact adoption</name>
  <files>bin/lib/ljx-lifecycle-shell-tools.cjs</files>
  <read_first>bin/lib/ljx-lifecycle-shell-tools.cjs, bin/lib/ljx-phase-context.cjs, .planning/phases/08-complete-core-lifecycle-shell/08-RESEARCH.md, LJX-GSD-CORE-COMMAND-SPECS.md</read_first>
  <action>Extend the shared lifecycle-shell helper so `plan-phase` can resolve a typed planning route for every accepted `phase_type`. Engineering phases should continue to support real GSD-style `PLAN.md` inventory generation. Discovery, refine, analysis, and paper phases should produce thin lifecycle `PLAN` wrappers that point to the accepted primary direct workflow and keep advanced direct tools visible. Experiment phases should distinguish planning from execution: adopt or create the thin lifecycle wrapper around `experiment-plan`, but leave actual execution to `experiment-bridge`. Add explicit direct-artifact adoption checks so existing domain artifacts can satisfy the plan step honestly; when adopted, record the adoption source and lifecycle-plan status in the phase record instead of rerunning the workflow by default.</action>
  <verify>node --test tests/plan-phase-shell.test.cjs</verify>
  <acceptance_criteria>
    - Plan-phase can resolve a deterministic planning route for all accepted phase types.
    - Existing valid direct-planning artifacts can be adopted into lifecycle truth without rerun.
    - Engineering planning remains GSD-like rather than being flattened into thin wrappers.
  </acceptance_criteria>
  <done>The lifecycle shell now knows how planning works for each typed phase and how to adopt valid direct artifacts safely.</done>
</task>

<task type="auto">
  <name>Task 2: Add a custom `ljx-GSD-plan-phase` builder and preview/install support</name>
  <files>bin/lib/codex-conversion.cjs, bin/lib/build-skills.cjs, bin/lib/manifest.cjs</files>
  <read_first>bin/lib/ljx-lifecycle-shell-tools.cjs, bin/lib/codex-conversion.cjs, bin/lib/build-skills.cjs, /Users/lijiaxin/.codex/get-shit-done/workflows/plan-phase.md</read_first>
  <action>Add a dedicated `buildPlanPhaseSkill` path that runs the lifecycle-shell helper first and then preserves the accepted shell behavior: research/context aware planning, phase-type-aware route selection, thin lifecycle wrapper creation for research-native phases, and explicit next-action recommendations. Keep stock GSD planning structure as the reference for engineering phases, but do not delegate blindly to stock GSD when typed route semantics differ. Update `build-skills.cjs` and `manifest.cjs` so `ljx-GSD-plan-phase` becomes bridge-ready only when the helper, builder, and runtime copy path all exist.</action>
  <verify>node bin/install.js --preview</verify>
  <acceptance_criteria>
    - The preview/install surface includes a generated `ljx-GSD-plan-phase` skill backed by the shared lifecycle helper.
    - The skill preserves the accepted shell distinction between planning and execution.
    - Plan-phase readiness in the manifest stays aligned with actual generated surface support.
  </acceptance_criteria>
  <done>`ljx-GSD-plan-phase` becomes a real bridge-ready lifecycle command on the preview surface.</done>
</task>

<task type="auto">
  <name>Task 3: Lock typed planning behavior with regression coverage</name>
  <files>tests/plan-phase-shell.test.cjs</files>
  <read_first>bin/lib/ljx-lifecycle-shell-tools.cjs, tests/discuss-phase-shell.test.cjs, .planning/phases/08-complete-core-lifecycle-shell/08-CONTEXT.md</read_first>
  <action>Add focused tests for: engineering route selection, discovery/refine/paper thin-wrapper planning, experiment planning-versus-execution separation, analysis defaulting to `claim-gate` as the primary shell route, adoption of existing direct planning artifacts, and honest-stop behavior on malformed config or unresolvable phases. Include preview-surface assumptions where useful, but keep the assertions tied to Phase 08 planning semantics instead of later execution or Phase 09 quality-gate rules.</action>
  <verify>node --test tests/plan-phase-shell.test.cjs && node bin/install.js --preview</verify>
  <acceptance_criteria>
    - Tests fail if typed planning routes drift from accepted defaults.
    - Tests fail if direct-artifact adoption silently reruns workflows or skips lifecycle-wrapper creation.
    - Tests fail if preview build plumbing omits the plan-phase runtime dependency chain.
  </acceptance_criteria>
  <done>The typed planning shell is protected by route/adoption regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/plan-phase-shell.test.cjs`
- [ ] `node bin/install.js --preview`
- [ ] `npm test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `ljx-GSD-plan-phase` becomes a phase-type-aware lifecycle planning entrypoint that preserves GSD structure and Auto workflow semantics
</success_criteria>

<output>
After completion, create `.planning/phases/08-complete-core-lifecycle-shell/08-02-SUMMARY.md`
</output>
