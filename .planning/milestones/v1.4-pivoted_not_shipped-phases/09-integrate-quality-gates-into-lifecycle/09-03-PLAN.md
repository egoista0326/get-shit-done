---
phase: "09-integrate-quality-gates-into-lifecycle"
plan: "03"
type: "execute"
wave: 3
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - "bin/lib/ljx-quality-gates-tools.cjs"
  - "bin/lib/ljx-verify-tools.cjs"
  - "bin/lib/ljx-state-tools.cjs"
  - "bin/lib/codex-conversion.cjs"
  - "bin/lib/build-skills.cjs"
  - "tests/verify-work-bridge.test.cjs"
  - "tests/runtime-shell.test.cjs"
  - "tests/lifecycle-next.test.cjs"
  - "tests/skill-build.test.cjs"
autonomous: true
requirements:
  - "IMPL-03"
user_setup: []
must_haves:
  truths:
    - "`verify-work` becomes an authoritative acceptance gate whose verdict, blockers, and evidence summary live in the phase record instead of being inferred from file existence alone."
    - "`next` and `progress` do not advance past a failed or blocked verification artifact just because it exists and is fresh."
    - "Installed `verify-work` skills preserve the same gate-state sync and honest-stop semantics as the repo-local runtime."
  artifacts:
    - "bin/lib/ljx-verify-tools.cjs` writes/reads structured `quality_gates.verify_work` summaries."
    - "bin/lib/ljx-state-tools.cjs routes from verification verdicts, blockers, and staleness instead of verification-file existence alone."
    - "tests/verify-work-bridge.test.cjs`, `tests/runtime-shell.test.cjs`, `tests/lifecycle-next.test.cjs`, and `tests/skill-build.test.cjs` prove acceptance-gate behavior."
  key_links:
    - "Engineering-style UAT mirrors remain secondary operator-facing artifacts, not the only verification truth."
    - "Later Auto research loops remain distinct tools; Phase 09 only makes `verify-work` an authoritative lifecycle gate."
---

<objective>
Integrate `ljx-GSD-verify-work` into authoritative lifecycle quality-gate state so continuation depends on acceptance verdicts, blockers, and fresh evidence instead of on artifact existence alone.

Purpose: close the final Phase 09 gap where review and verification artifacts can exist but the lifecycle shell still lacks authoritative gate truth for continuation.
Output: structured verify-work gate-state persistence, verification-aware lifecycle routing, installed-skill alignment, and regression coverage for failed-versus-ready verification behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-integrate-quality-gates-into-lifecycle/09-CONTEXT.md
@.planning/phases/09-integrate-quality-gates-into-lifecycle/09-RESEARCH.md
@LJX-GSD-CORE-COMMAND-SPECS.md
@LJX-GSD-INTERFACES.md
@bin/lib/ljx-quality-gates-tools.cjs
@bin/lib/ljx-verify-tools.cjs
@bin/lib/ljx-state-tools.cjs
@bin/lib/codex-conversion.cjs
@bin/lib/build-skills.cjs
@/Users/lijiaxin/.codex/get-shit-done/workflows/verify-work.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist authoritative verify-work gate summaries into phase records</name>
  <files>bin/lib/ljx-quality-gates-tools.cjs, bin/lib/ljx-verify-tools.cjs</files>
  <read_first>bin/lib/ljx-quality-gates-tools.cjs, bin/lib/ljx-verify-tools.cjs, LJX-GSD-CORE-COMMAND-SPECS.md, /Users/lijiaxin/.codex/get-shit-done/workflows/verify-work.md</read_first>
  <action>Extend the shared quality-gate helper and `ljx-verify-tools.cjs` so verification can publish authoritative `quality_gates.verify_work` summaries into phase records. The persisted summary should include at least: gate status, verdict, blocking reasons, warning reasons, artifact path, evidence files, and any engineering-only UAT mirror path. Preserve the accepted broader evidence-gate semantics: engineering phases may still emit `UAT.md`, but `VERIFICATION.md` plus the structured summary remain the authoritative gate truth.</action>
  <verify>node --test tests/verify-work-bridge.test.cjs</verify>
  <acceptance_criteria>
    - `verify-work` exposes a structured phase-record update payload for the verification gate.
    - The persisted verification summary captures readiness versus blocked/fix/replan states.
    - Engineering UAT mirrors remain optional secondary artifacts rather than replacing verification truth.
  </acceptance_criteria>
  <done>Verification becomes a structured authoritative gate rather than an artifact-only outcome.</done>
</task>

<task type="auto">
  <name>Task 2: Make lifecycle routing respect verification verdicts instead of file existence</name>
  <files>bin/lib/ljx-state-tools.cjs, tests/runtime-shell.test.cjs, tests/lifecycle-next.test.cjs</files>
  <read_first>bin/lib/ljx-state-tools.cjs, bin/lib/ljx-verify-tools.cjs, tests/runtime-shell.test.cjs, tests/lifecycle-next.test.cjs</read_first>
  <action>Update `ljx-state-tools.cjs` so current-phase and next-phase routing reads the authoritative verification summary first. Required behavior: a failed or blocked verification artifact must not be treated as equivalent to a successful one just because the file exists and is fresh; stale verification should route back to `verify-work`; blocked verification should recommend the corrective action implied by the recorded blockers; only a ready verification verdict may allow progression beyond the phase.</action>
  <verify>node --test tests/verify-work-bridge.test.cjs tests/runtime-shell.test.cjs tests/lifecycle-next.test.cjs</verify>
  <acceptance_criteria>
    - `next` and `progress` do not skip over failed or blocked verification results.
    - Freshness and verdict are both respected.
    - Phase 09 routing remains additive by falling back honestly when older phases lack structured verification summaries.
  </acceptance_criteria>
  <done>Lifecycle routing now treats verification as an authoritative acceptance gate.</done>
</task>

<task type="auto">
  <name>Task 3: Align installed `verify-work` skills and preview runtime with authoritative verification-gate state</name>
  <files>bin/lib/codex-conversion.cjs, bin/lib/build-skills.cjs, tests/skill-build.test.cjs</files>
  <read_first>bin/lib/ljx-verify-tools.cjs, bin/lib/codex-conversion.cjs, bin/lib/build-skills.cjs, tests/skill-build.test.cjs</read_first>
  <action>Update generated `verify-work` skill prompts so installed skills explicitly sync `quality_gates.verify_work` after writing `VERIFICATION.md`. If the Phase 09 shared helper is new, ensure preview/install copies it into the runtime tree. Add regression coverage proving installed skills still point at the correct runtime helpers and preserve the accepted stop-and-explain gate semantics.</action>
  <verify>node bin/install.js --preview && node --test tests/skill-build.test.cjs</verify>
  <acceptance_criteria>
    - Installed `verify-work` skills point at the correct runtime helper chain.
    - Preview installs include any new shared Phase 09 helper module.
    - Installed-skill behavior remains aligned with repo-local verification-gate semantics.
  </acceptance_criteria>
  <done>Preview/install behavior preserves the authoritative verification-gate contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/verify-work-bridge.test.cjs tests/runtime-shell.test.cjs tests/lifecycle-next.test.cjs tests/skill-build.test.cjs`
- [ ] `node bin/install.js --preview`
- [ ] `npm test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `verify-work` becomes an authoritative lifecycle acceptance gate and only successful verification allows continuation
</success_criteria>

<output>
After completion, create `.planning/phases/09-integrate-quality-gates-into-lifecycle/09-03-SUMMARY.md`
</output>
