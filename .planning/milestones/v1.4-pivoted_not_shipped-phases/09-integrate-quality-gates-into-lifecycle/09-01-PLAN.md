---
phase: "09-integrate-quality-gates-into-lifecycle"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "bin/lib/ljx-quality-gates-tools.cjs"
  - "bin/lib/ljx-code-review-tools.cjs"
  - "bin/lib/ljx-state-tools.cjs"
  - "tests/code-review-bridge.test.cjs"
  - "tests/runtime-shell.test.cjs"
  - "tests/lifecycle-next.test.cjs"
autonomous: true
requirements:
  - "IMPL-03"
user_setup: []
must_haves:
  truths:
    - "Phase-level code review becomes an authoritative quality gate summarized in the phase record instead of an artifact-only sidecar."
    - "`next` and `progress` route from structured code-review verdict and freshness state before they consider later lifecycle moves."
    - "Phase 09 reuses the existing code-review helper surface instead of inventing a second review command family."
  artifacts:
    - "bin/lib/ljx-quality-gates-tools.cjs` (or equivalent shared helper) persists and reads `quality_gates.code_review` summaries."
    - "bin/lib/ljx-code-review-tools.cjs exposes the structured gate-update payload alongside the phase-local artifact path."
    - "tests/code-review-bridge.test.cjs`, `tests/runtime-shell.test.cjs`, and `tests/lifecycle-next.test.cjs` prove code-review gate routing and blocked-next behavior."
  key_links:
    - "ljx-state-tools.cjs consumes shared code-review gate summaries instead of only checking artifact existence."
    - "Existing review requirement rules in ljx-bridge-contract.cjs remain authoritative and are not redefined in this phase."
---

<objective>
Make `ljx-GSD-code-review` a real lifecycle-owned post-execute gate by persisting its summarized truth into the authoritative phase record and by making lifecycle routing consume that truth.

Purpose: stop treating code review as a mere file-presence sidecar and start treating it as the canonical first quality gate after execution.
Output: shared code-review gate-state helpers, updated lifecycle routing, and focused regression coverage proving blocked reviews stop continuation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-integrate-quality-gates-into-lifecycle/09-CONTEXT.md
@.planning/phases/09-integrate-quality-gates-into-lifecycle/09-RESEARCH.md
@LJX-GSD-EXTENDED-PUBLIC-COMMAND-SPECS.md
@LJX-GSD-DESIGN-DECISION-LOG.md
@bin/lib/ljx-code-review-tools.cjs
@bin/lib/ljx-state-tools.cjs
@bin/lib/ljx-lifecycle-shell-tools.cjs
@/Users/lijiaxin/.codex/get-shit-done/workflows/code-review.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a shared code-review quality-gate state helper</name>
  <files>bin/lib/ljx-quality-gates-tools.cjs</files>
  <read_first>bin/lib/ljx-runtime-state.cjs, bin/lib/ljx-code-review-tools.cjs, bin/lib/ljx-verify-tools.cjs, .planning/phases/09-integrate-quality-gates-into-lifecycle/09-RESEARCH.md</read_first>
  <action>Create one shared helper module for Phase 09 quality-gate state. It should read and write phase-record-local `quality_gates.code_review` summaries without creating a new top-level state family. Encode the minimum routing truth needed for lifecycle progression: gate status, overall verdict, artifact path, freshness policy, reviewed scope, blocking/warning counts, and rerun policy. Keep artifact mtimes and reviewed-file existence as evidence anchors; do not invent a second review-state control plane.</action>
  <verify>node --test tests/code-review-bridge.test.cjs</verify>
  <acceptance_criteria>
    - A shared helper can persist and read `quality_gates.code_review` in phase records.
    - The stored summary is enough for later routing without re-parsing all review prose when state exists.
    - The helper preserves honest-stop behavior on missing planning or malformed phase-record JSON.
  </acceptance_criteria>
  <done>The repository has one reusable code-review gate-state helper that later routing code can consume.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint code-review context and post-execute routing onto authoritative review-gate state</name>
  <files>bin/lib/ljx-code-review-tools.cjs, bin/lib/ljx-state-tools.cjs</files>
  <read_first>bin/lib/ljx-quality-gates-tools.cjs, bin/lib/ljx-code-review-tools.cjs, bin/lib/ljx-state-tools.cjs, LJX-GSD-EXTENDED-PUBLIC-COMMAND-SPECS.md</read_first>
  <action>Update `ljx-code-review-tools.cjs` so the helper returns both the phase-local review artifact path and a structured phase-record quality-gate update payload for `code_review`. Then update `ljx-state-tools.cjs` so post-execute routing and `next` / `progress` read the structured code-review summary first. Required behavior: missing or stale required review recommends `ljx-GSD-code-review`; blocking findings recommend `ljx-GSD-code-review-fix`; only fresh, non-blocking review may permit movement to verification.</action>
  <verify>node --test tests/code-review-bridge.test.cjs tests/runtime-shell.test.cjs tests/lifecycle-next.test.cjs</verify>
  <acceptance_criteria>
    - `ljx-code-review-tools.cjs` exports enough information for the installed skill to sync authoritative review state.
    - `ljx-state-tools.cjs` stops treating review-file existence alone as sufficient for continuation.
    - Blocked or warning-only review verdicts produce the accepted next-step recommendations.
  </acceptance_criteria>
  <done>Code-review becomes a real lifecycle gate in routing, not just an optional artifact check.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for authoritative code-review routing</name>
  <files>tests/code-review-bridge.test.cjs, tests/runtime-shell.test.cjs, tests/lifecycle-next.test.cjs</files>
  <read_first>tests/code-review-bridge.test.cjs, tests/runtime-shell.test.cjs, tests/lifecycle-next.test.cjs, bin/lib/ljx-state-tools.cjs</read_first>
  <action>Add failing-first coverage for the Phase 09 regression boundary: lifecycle routing must not advance past a fresh-but-blocked code review, must respect phase-record review summaries when present, and must still fall back honestly to artifact-backed freshness checks when summaries are absent. Keep the tests focused on current accepted contracts rather than later verify-work semantics.</action>
  <verify>node --test tests/code-review-bridge.test.cjs tests/runtime-shell.test.cjs tests/lifecycle-next.test.cjs</verify>
  <acceptance_criteria>
    - Tests fail if `next` or `progress` skip over blocking review findings.
    - Tests fail if structured review summaries are ignored when present.
    - Tests still pass for artifact-only fallback cases so Phase 09 remains additive rather than breaking old data.
  </acceptance_criteria>
  <done>Regression coverage locks in authoritative code-review gate routing.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/code-review-bridge.test.cjs tests/runtime-shell.test.cjs tests/lifecycle-next.test.cjs`
- [ ] `npm test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `code-review` becomes an authoritative post-execute lifecycle gate consumed by `next` and `progress`
</success_criteria>

<output>
After completion, create `.planning/phases/09-integrate-quality-gates-into-lifecycle/09-01-SUMMARY.md`
</output>
