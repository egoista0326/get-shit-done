# Phase 20 Plan 03: Scenario Probes And Regression Coverage

## Goal

Run richer dynamic and static probes than the previous clean-round closure.

## Probe Families

- full test suite
- preview install and manifest print
- current `progress` and `next` helper routing
- strict focused contract suite
- generated skill contract grep/probe checks
- config alias fixture tests
- source-root fallback tests
- migration/admin/workstream lifecycle tests
- research helper contract tests for experiment, claim, review, novelty, paper/rebuttal where available

## Verification

- All required commands either pass or produce a documented environment-only failure.
- Any failing repository logic becomes a candidate bug for second-pass confirmation.
