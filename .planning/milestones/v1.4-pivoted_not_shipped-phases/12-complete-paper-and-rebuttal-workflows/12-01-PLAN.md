---
phase: 12-complete-paper-and-rebuttal-workflows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/ljx-paper-evidence-tools.cjs
  - bin/lib/ljx-paper-pipeline-tools.cjs
  - tests/paper-evidence-tools.test.cjs
  - tests/paper-pipeline-bridge.test.cjs
  - .planning/phases/12-complete-paper-and-rebuttal-workflows/12-01-SUMMARY.md
autonomous: true
requirements:
  - IMPL-06
requirements_addressed:
  - IMPL-06
user_setup: []
must_haves:
  truths:
    - "`paper-pipeline` owns phase-local PAPER_PLAN/PAPER_PIPELINE plus the bounded paper workspace."
    - "Paper context reads expose intended paper-state and artifact links without creating empty `.planning/state/papers/*.json` files."
    - "Paper-state writes are explicit, validated, and field-preserving."
    - "Malformed, non-object, missing/falsy phase_id, and mismatched phase_id existing paper records stop honestly before write."
  artifacts:
    - path: "bin/lib/ljx-paper-evidence-tools.cjs"
      provides: "Shared paper/rebuttal state path, artifact link, readiness, and writer validation rules"
      exports:
        - "PAPER_ARTIFACTS"
        - "REBUTTAL_ARTIFACTS"
        - "PAPER_LINK_STATUSES"
        - "buildPaperStatePaths"
        - "buildPaperArtifactLinks"
        - "buildRebuttalArtifactLinks"
        - "buildPaperReadinessSummary"
        - "buildPaperWorkflowContext"
        - "validatePaperStateRecordForWrite"
    - path: "bin/lib/ljx-paper-pipeline-tools.cjs"
      provides: "Paper-pipeline context and explicit validated paper-state writer"
      contains: "writePaperPipelineState"
  key_links:
    - from: "bin/lib/ljx-paper-pipeline-tools.cjs"
      to: "bin/lib/ljx-paper-evidence-tools.cjs"
      via: "buildPaperWorkflowContext(projectRoot, baseContext)"
      pattern: "buildPaperWorkflowContext"
    - from: "bin/lib/ljx-paper-pipeline-tools.cjs"
      to: "bin/lib/ljx-runtime-state.cjs"
      via: "updateStateRecord(projectRoot, 'papers', phaseId, updater)"
      pattern: "updateStateRecord"
---

<objective>
Complete `paper-pipeline` stage ownership and paper-state links while preserving Auto paper-writing semantics.

Purpose: Phase 12 needs `paper-pipeline` to own a bounded paper workspace and a coherent paper-state link record without overwriting future rebuttal state, fabricating state on context reads, or flattening Auto's paper-writing stage sequence.

Output: A shared paper-state helper, updated paper-pipeline context/writer behavior, tests that lock no-empty-state reads and writer safety, and a plan summary.
</objective>

<execution_context>
@/Users/lijiaxin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lijiaxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/IMPLEMENTATION-LESSONS.md
@.planning/phases/12-complete-paper-and-rebuttal-workflows/12-CONTEXT.md
@.planning/phases/12-complete-paper-and-rebuttal-workflows/12-RESEARCH.md
@.planning/phases/11-complete-experiment-review-and-claim-workflows/11-RESEARCH.md
@bin/lib/ljx-paper-pipeline-tools.cjs
@bin/lib/ljx-runtime-state.cjs
@bin/lib/ljx-phase-context.cjs
@bin/lib/ljx-experiment-evidence-tools.cjs
@tests/paper-pipeline-bridge.test.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify paper-state helper and paper-pipeline writer safety</name>
  <read_first>
    .planning/phases/12-complete-paper-and-rebuttal-workflows/12-CONTEXT.md
    .planning/phases/12-complete-paper-and-rebuttal-workflows/12-RESEARCH.md
    bin/lib/ljx-paper-pipeline-tools.cjs
    bin/lib/ljx-runtime-state.cjs
    bin/lib/ljx-experiment-evidence-tools.cjs
    tests/paper-pipeline-bridge.test.cjs
  </read_first>
  <files>tests/paper-evidence-tools.test.cjs, tests/paper-pipeline-bridge.test.cjs</files>
  <behavior>
    - `buildPaperStatePaths(projectRoot, '12')` returns `.planning/state/papers/12.json` and `paperStateFamily: 'papers'`.
    - `buildPaperArtifactLinks(baseContext)` returns `paperPlan` and `paperPipeline` links from the phase directory.
    - `buildPaperWorkflowContext(projectRoot, baseContext)` returns `paperStateRecordPath`, `paperArtifactLinks`, `rebuttalArtifactLinks`, and `paperReadiness` without writing any paper-state file.
    - `readPaperPipelineContext()` exposes the shared paper context fields while preserving `paperWorkspacePath`, `companionSkills`, `downstreamTools`, `defaultVenue`, `maxPages`, `autoProceed`, and `humanCheckpoint`.
    - `writePaperPipelineState()` rejects malformed JSON, non-object JSON, missing/falsy `phase_id`, and mismatched `phase_id` in an existing `papers/{phase}.json` record.
    - `writePaperPipelineState()` preserves existing `rebuttal` or other sibling state fields when updating paper-pipeline-owned state.
  </behavior>
  <action>
    Add failing tests first. Create `tests/paper-evidence-tools.test.cjs` for the new helper contract and no-empty-state rule. Extend `tests/paper-pipeline-bridge.test.cjs` to assert paper context fields, no state file creation during `context`, validation rejection for malformed/mismatched existing state, and sibling-field preservation by seeding an existing paper-state record that contains `rebuttal`.
  </action>
  <verify>
    <automated>node --test tests/paper-evidence-tools.test.cjs tests/paper-pipeline-bridge.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    Tests fail before implementation because the shared paper helper does not exist and the current writer overwrites existing state.
  </acceptance_criteria>
  <done>Tests encode D-08 through D-17 and D-28 through D-31 for paper-pipeline ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared paper-state helper</name>
  <read_first>
    bin/lib/ljx-runtime-state.cjs
    bin/lib/ljx-experiment-evidence-tools.cjs
    tests/paper-evidence-tools.test.cjs
  </read_first>
  <files>bin/lib/ljx-paper-evidence-tools.cjs</files>
  <behavior>
    - Helper path construction uses `buildStateRecordPath(projectRoot, 'papers', phaseId)`.
    - Helper links use resolved `baseContext.phase.path`, not user-supplied arbitrary paths.
    - Helper reads do not import or call `writeStateRecord()` or `updateStateRecord()`.
    - Writer validation uses `readStateRecord()` and rejects malformed, non-object, missing/falsy phase IDs, and mismatched phase IDs before updates occur.
  </behavior>
  <action>
    Add `bin/lib/ljx-paper-evidence-tools.cjs` as a small CommonJS helper. Export the constants and functions listed in frontmatter. Keep status vocabulary simple: `present`, `missing`, `needs_confirmation`, and `stale` where applicable. Include paper links for `PAPER_PLAN` and `PAPER_PIPELINE`, plus rebuttal links for `REVIEWS_RAW`, `ISSUE_BOARD`, `STRATEGY_PLAN`, `REBUTTAL_EXPERIMENT_PLAN`, `REBUTTAL_EXPERIMENTS`, `REBUTTAL_DRAFT_v1`, `PASTE_READY`, `REBUTTAL_DRAFT_rich`, `MCP_STRESS_TEST`, `REBUTTAL_STATE`, and `FOLLOWUP_LOG`. `buildPaperReadinessSummary()` should expose missing paper artifacts and mark rebuttal paste-ready finalization as `needs_confirmation` when venue limits are not confirmed.
  </action>
  <verify>
    <automated>node --check bin/lib/ljx-paper-evidence-tools.cjs && node --test tests/paper-evidence-tools.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    Shared helper tests pass and the helper remains a link/validation layer only.
  </acceptance_criteria>
  <done>Shared paper/rebuttal state semantics are centralized without adding a control plane.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update paper-pipeline context and writer</name>
  <read_first>
    bin/lib/ljx-paper-pipeline-tools.cjs
    bin/lib/ljx-paper-evidence-tools.cjs
    bin/lib/ljx-runtime-state.cjs
    tests/paper-pipeline-bridge.test.cjs
  </read_first>
  <files>bin/lib/ljx-paper-pipeline-tools.cjs</files>
  <behavior>
    - `readPaperPipelineContext()` spreads `buildPaperWorkflowContext(projectRoot, baseContext)` into `extraFields`.
    - `writePaperPipelineState()` calls `validatePaperStateRecordForWrite()` before updating.
    - `writePaperPipelineState()` uses `updateStateRecord()` and stores the incoming payload under a paper-pipeline-owned field while preserving existing sibling fields.
    - Existing CLI behavior for `context` and `write-paper-state --payload-file` remains supported.
  </behavior>
  <action>
    Modify `bin/lib/ljx-paper-pipeline-tools.cjs` to import `buildPaperWorkflowContext` and `validatePaperStateRecordForWrite`. Replace fresh-record `writeStateRecord()` behavior with validated `updateStateRecord()` behavior. Use a predictable owner field such as `paperPipeline` and include `phase_id`, `updatedBy: 'paper-pipeline'`, and `updatedAt`. Preserve existing `phase_id` and sibling fields.
  </action>
  <verify>
    <automated>node --check bin/lib/ljx-paper-pipeline-tools.cjs && node --test tests/paper-evidence-tools.test.cjs tests/paper-pipeline-bridge.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    Paper-pipeline helper exposes the shared paper context, never writes state during context reads, rejects unsafe existing records, and preserves rebuttal-owned state.
  </acceptance_criteria>
  <done>`paper-pipeline` owns its bounded workspace and paper-state updates safely.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | `papers/{phase}.json` update | mitigate | Validate existing state before writing and use field-preserving updates. |
| T-12-01-02 | Repudiation | Context read state provenance | mitigate | Tests assert context reads do not create empty paper-state files. |
| T-12-01-03 | Spoofing | Paper artifact links | mitigate | Build links from resolved phase context only. |
| T-12-01-04 | Information Disclosure | Helper context payload | accept | This plan exposes local paths already used by existing helpers; no new external transmission is introduced. |
</threat_model>

<verification>
Run:
```bash
node --check bin/lib/ljx-paper-evidence-tools.cjs
node --check bin/lib/ljx-paper-pipeline-tools.cjs
node --test tests/paper-evidence-tools.test.cjs tests/paper-pipeline-bridge.test.cjs
git diff --check
```
</verification>

<success_criteria>
Plan 12-01 is complete when `paper-pipeline` has coherent bounded paper workspace context, shared paper-state links, and a validated field-preserving paper-state writer.
</success_criteria>

<output>
After completion, create `.planning/phases/12-complete-paper-and-rebuttal-workflows/12-01-SUMMARY.md`.
</output>
