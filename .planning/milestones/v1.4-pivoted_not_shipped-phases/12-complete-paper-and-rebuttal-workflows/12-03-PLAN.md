---
phase: 12-complete-paper-and-rebuttal-workflows
plan: 03
type: execute
wave: 3
depends_on:
  - "12-01"
  - "12-02"
files_modified:
  - bin/lib/codex-conversion.cjs
  - bin/lib/build-skills.cjs
  - tests/skill-build.test.cjs
  - tests/execute-phase-shell.test.cjs
  - tests/lifecycle-next.test.cjs
  - .planning/phases/12-complete-paper-and-rebuttal-workflows/12-03-SUMMARY.md
autonomous: true
requirements:
  - IMPL-06
requirements_addressed:
  - IMPL-06
user_setup: []
must_haves:
  truths:
    - "Generated `paper-pipeline` skill wording preserves Auto paper stages and uses the explicit writer command."
    - "Generated `rebuttal` skill wording preserves Auto rebuttal stages and uses the explicit rebuttal writer command."
    - "Preview/install output copies any runtime helper needed by the generated paper/rebuttal skills."
    - "Paper and rebuttal routing remains recommendation-only and does not mutate lifecycle completion."
    - "Direct paper lifecycle still requires `PAPER_PIPELINE` rather than treating optional `PAPER_PLAN` as execution completion."
  artifacts:
    - path: "bin/lib/codex-conversion.cjs"
      provides: "Generated paper/rebuttal skill instructions aligned with runtime writers and venue/evidence constraints"
      contains: "write-rebuttal-state"
    - path: "bin/lib/build-skills.cjs"
      provides: "Installed runtime helper copy support"
      contains: "ljx-paper-evidence-tools"
  key_links:
    - from: "bin/lib/build-skills.cjs"
      to: "bin/lib/ljx-paper-evidence-tools.cjs"
      via: "runtime helper copy"
      pattern: "installedPaperEvidenceHelperPath"
    - from: "bin/lib/codex-conversion.cjs"
      to: "bin/lib/ljx-rebuttal-tools.cjs"
      via: "generated explicit writer command"
      pattern: "write-rebuttal-state"
---

<objective>
Integrate paper/rebuttal routing with venue and evidence constraints across generated skills, install output, and lifecycle-facing tests.

Purpose: Runtime helper semantics are not complete until the public skill surface and install preview match them. Phase 12 must keep Auto/GSD reuse visible, preserve minimal modification, and avoid direct-workflow helper drift.

Output: Updated generated paper/rebuttal skill wording, installed runtime copy support for the shared paper helper, focused tests for generated skill/install/lifecycle routing, full regression, and a plan summary.
</objective>

<execution_context>
@/Users/lijiaxin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lijiaxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/IMPLEMENTATION-LESSONS.md
@.planning/phases/12-complete-paper-and-rebuttal-workflows/12-CONTEXT.md
@.planning/phases/12-complete-paper-and-rebuttal-workflows/12-RESEARCH.md
@.planning/phases/12-complete-paper-and-rebuttal-workflows/12-01-SUMMARY.md
@.planning/phases/12-complete-paper-and-rebuttal-workflows/12-02-SUMMARY.md
@bin/lib/codex-conversion.cjs
@bin/lib/build-skills.cjs
@bin/lib/ljx-lifecycle-shell-tools.cjs
@bin/lib/manifest.cjs
@tests/skill-build.test.cjs
@tests/execute-phase-shell.test.cjs
@tests/lifecycle-next.test.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify generated skill and install surface alignment</name>
  <read_first>
    bin/lib/codex-conversion.cjs
    bin/lib/build-skills.cjs
    tests/skill-build.test.cjs
    /Users/lijiaxin/.codex/skills/paper-writing/SKILL.md
    /Users/lijiaxin/.codex/skills/rebuttal/SKILL.md
  </read_first>
  <files>tests/skill-build.test.cjs</files>
  <behavior>
    - Generated `ljx-GSD-paper-pipeline` includes the full command `write-paper-state --cwd "$PWD" {{GSD_ARGS}} --payload-file <payload.json>`.
    - Generated `ljx-GSD-paper-pipeline` names `paper-plan`, `paper-figure`, `paper-illustration`, `paper-write`, `paper-compile`, and `auto-paper-improvement-loop`.
    - Generated `ljx-GSD-rebuttal` includes the full command `write-rebuttal-state --cwd "$PWD" {{GSD_ARGS}} --payload-file <payload.json>`.
    - Generated `ljx-GSD-rebuttal` names review normalization, issue board, strategy, experiment plan/log, draft, paste-ready output, rich draft, stress test, state, and follow-up.
    - Generated rebuttal wording preserves missing-review stop, workspace resume, venue confirmation, and explicit experiment-bridge recommendation semantics.
    - Preview/install runtime output copies `ljx-paper-evidence-tools.cjs` if it exists.
  </behavior>
  <action>
    Extend `tests/skill-build.test.cjs` first. Add assertions for the full writer commands and Auto stage language. Add installed runtime helper assertion for `ljx-paper-evidence-tools.cjs` and require it to confirm `buildPaperWorkflowContext` is exported. Keep existing assertions for paper/rebuttal manifest readiness, helper paths, and no direct `.planning/state` writes.
  </action>
  <verify>
    <automated>node --test tests/skill-build.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    Tests fail until generated skill wording and install copy support match runtime behavior.
  </acceptance_criteria>
  <done>Generated/installed surface expectations are locked before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update generated skills and install copy support</name>
  <read_first>
    bin/lib/codex-conversion.cjs
    bin/lib/build-skills.cjs
    bin/lib/ljx-paper-evidence-tools.cjs
    tests/skill-build.test.cjs
  </read_first>
  <files>bin/lib/codex-conversion.cjs, bin/lib/build-skills.cjs</files>
  <behavior>
    - `buildPaperPipelineSkill()` documents the full explicit paper-state writer command, bounded `paper/` workspace, venue/page/improvement settings, and Auto paper stages.
    - `buildRebuttalSkill()` documents the full explicit rebuttal-state writer command, bounded `rebuttal/` workspace, venue character-limit confirmation, workspace resume, and Auto rebuttal stages.
    - `buildBridgeReadySkills()` copies `bin/lib/ljx-paper-evidence-tools.cjs` into installed runtime output.
    - No generated skill text tells agents to write `.planning/state/` directly.
  </behavior>
  <action>
    Update `bin/lib/codex-conversion.cjs` paper and rebuttal builder functions. Update `bin/lib/build-skills.cjs` to add an installed helper path and copy for `ljx-paper-evidence-tools.cjs` next to paper-pipeline/rebuttal helpers. Keep existing helper copy names and Auto skill content preservation logic intact.
  </action>
  <verify>
    <automated>node --check bin/lib/codex-conversion.cjs && node --check bin/lib/build-skills.cjs && node --test tests/skill-build.test.cjs && node bin/install.js --preview</automated>
  </verify>
  <acceptance_criteria>
    Generated skills and installed runtime surface match the Phase 12 helper semantics and preserve Auto paper/rebuttal intent.
  </acceptance_criteria>
  <done>Public skill/install surface is aligned with runtime writer and venue/evidence constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify lifecycle-facing routing constraints</name>
  <read_first>
    bin/lib/ljx-lifecycle-shell-tools.cjs
    tests/execute-phase-shell.test.cjs
    tests/lifecycle-next.test.cjs
    bin/lib/ljx-paper-pipeline-tools.cjs
    bin/lib/ljx-rebuttal-tools.cjs
  </read_first>
  <files>tests/execute-phase-shell.test.cjs, tests/lifecycle-next.test.cjs</files>
  <behavior>
    - Paper phase execution still routes primary direct workflow through `ljx-GSD-paper-pipeline`.
    - Advanced direct tools still include `ljx-GSD-rebuttal`.
    - Direct workflow artifact completion still requires `PAPER_PIPELINE`; optional `PAPER_PLAN` alone is not execution completion.
    - `next` remains honest and one-step; it recommends, not runs, downstream paper/rebuttal workflows.
  </behavior>
  <action>
    Add or strengthen tests only where current coverage is ambiguous. If existing tests already cover a behavior, add an assertion near that test instead of duplicating fixtures. Avoid lifecycle implementation changes unless a test exposes real drift from Phase 12 requirements.
  </action>
  <verify>
    <automated>node --test tests/execute-phase-shell.test.cjs tests/lifecycle-next.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    Lifecycle-facing tests confirm paper/rebuttal routing remains explicit and does not mutate completion outside the lifecycle shell.
  </acceptance_criteria>
  <done>Routing constraints are verified without introducing a second paper/rebuttal controller.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Tampering | Generated skill state instructions | mitigate | Generated text uses explicit writer commands and forbids direct `.planning/state` writes. |
| T-12-03-02 | Repudiation | Install/runtime copy drift | mitigate | Preview install test requires the shared paper helper export in installed runtime. |
| T-12-03-03 | Spoofing | Lifecycle completion inference | mitigate | Tests keep `PAPER_PIPELINE` as required execution artifact and keep `next` one-step. |
| T-12-03-04 | Elevation of Privilege | Hidden rebuttal experiment execution | mitigate | Generated rebuttal text keeps experiment bridge as an explicit recommendation unless configured and bounded. |
</threat_model>

<verification>
Run:
```bash
node --check bin/lib/codex-conversion.cjs
node --check bin/lib/build-skills.cjs
node --test tests/skill-build.test.cjs
node --test tests/execute-phase-shell.test.cjs tests/lifecycle-next.test.cjs
node bin/install.js --preview
npm test
git diff --check
```
</verification>

<success_criteria>
Plan 12-03 is complete when generated skills, preview/install output, and lifecycle-facing routing all match Phase 12's bounded paper/rebuttal workflow model.
</success_criteria>

<output>
After completion, create `.planning/phases/12-complete-paper-and-rebuttal-workflows/12-03-SUMMARY.md`.
</output>
