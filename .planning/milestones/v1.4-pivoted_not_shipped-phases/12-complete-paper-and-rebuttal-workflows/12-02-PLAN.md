---
phase: 12-complete-paper-and-rebuttal-workflows
plan: 02
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - bin/lib/ljx-rebuttal-tools.cjs
  - tests/rebuttal-bridge.test.cjs
  - .planning/phases/12-complete-paper-and-rebuttal-workflows/12-02-SUMMARY.md
autonomous: true
requirements:
  - IMPL-06
requirements_addressed:
  - IMPL-06
user_setup: []
must_haves:
  truths:
    - "`rebuttal` owns the bounded `rebuttal/` workspace and preserves Auto rebuttal stage semantics."
    - "Missing root reviews remain an honest stop for initial rebuttal."
    - "Existing rebuttal workspace artifacts still allow resume or follow-up."
    - "`rebuttal` links into the same `papers/{phase}.json` record through an explicit writer that preserves paper-pipeline-owned fields."
    - "Missing or unconfirmed venue character-limit constraints are surfaced before paste-ready finalization."
  artifacts:
    - path: "bin/lib/ljx-rebuttal-tools.cjs"
      provides: "Rebuttal context and explicit rebuttal paper-state writer"
      contains: "writeRebuttalState"
  key_links:
    - from: "bin/lib/ljx-rebuttal-tools.cjs"
      to: "bin/lib/ljx-paper-evidence-tools.cjs"
      via: "buildPaperWorkflowContext(projectRoot, baseContext)"
      pattern: "buildPaperWorkflowContext"
    - from: "bin/lib/ljx-rebuttal-tools.cjs"
      to: "bin/lib/ljx-runtime-state.cjs"
      via: "updateStateRecord(projectRoot, 'papers', phaseId, updater)"
      pattern: "write-rebuttal-state"
---

<objective>
Complete `rebuttal` workspace and rebuttal-state ownership while keeping it inside the shared paper-state model.

Purpose: Phase 12 needs rebuttal to continue/resume from the bounded `rebuttal/` workspace and update paper/rebuttal linkage without inventing a second submission state family or weakening venue/evidence constraints.

Output: Updated rebuttal helper context, explicit `write-rebuttal-state` command, regression tests for missing-review stop, resume behavior, venue confirmation, and paper-state preservation, plus a plan summary.
</objective>

<execution_context>
@/Users/lijiaxin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lijiaxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/IMPLEMENTATION-LESSONS.md
@.planning/phases/12-complete-paper-and-rebuttal-workflows/12-CONTEXT.md
@.planning/phases/12-complete-paper-and-rebuttal-workflows/12-RESEARCH.md
@.planning/phases/12-complete-paper-and-rebuttal-workflows/12-01-SUMMARY.md
@bin/lib/ljx-paper-evidence-tools.cjs
@bin/lib/ljx-rebuttal-tools.cjs
@bin/lib/ljx-runtime-state.cjs
@tests/rebuttal-bridge.test.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify rebuttal shared-state and venue behavior</name>
  <read_first>
    .planning/phases/12-complete-paper-and-rebuttal-workflows/12-CONTEXT.md
    .planning/phases/12-complete-paper-and-rebuttal-workflows/12-RESEARCH.md
    bin/lib/ljx-rebuttal-tools.cjs
    tests/rebuttal-bridge.test.cjs
  </read_first>
  <files>tests/rebuttal-bridge.test.cjs</files>
  <behavior>
    - `readRebuttalContext()` exposes `paperStateRecordPath`, `paperArtifactLinks`, `rebuttalArtifactLinks`, and `paperReadiness`.
    - Initial rebuttal without review artifacts still returns `stopReasonCode: 'missing_review_artifacts'`.
    - Existing `rebuttal/REVIEWS_RAW.md` still resumes even if root review files are absent.
    - Missing or unconfirmed `research.rebuttal.characterLimit` sets `requiresVenueRuleConfirmation: true` and a readiness status that prevents claiming paste-ready finalization.
    - `writeRebuttalState()` and CLI `write-rebuttal-state --payload-file` write rebuttal-owned state under `papers/{phase}.json` while preserving `paperPipeline`.
    - Unsafe existing paper records are rejected with honest errors before write.
  </behavior>
  <action>
    Extend `tests/rebuttal-bridge.test.cjs` first. Add tests for shared paper context fields, no paper-state creation during context reads, writer creation, CLI payload-file support, preserving existing `paperPipeline`, rejecting malformed/mismatched/missing/falsy `phase_id`, and venue confirmation readiness. Keep existing missing-review and workspace-resume tests intact.
  </action>
  <verify>
    <automated>node --test tests/rebuttal-bridge.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    Tests fail until rebuttal imports the shared helper and gains the explicit writer.
  </acceptance_criteria>
  <done>Rebuttal tests encode D-18 through D-23 and D-31.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement rebuttal context integration and writer</name>
  <read_first>
    bin/lib/ljx-paper-evidence-tools.cjs
    bin/lib/ljx-rebuttal-tools.cjs
    bin/lib/ljx-runtime-state.cjs
    tests/rebuttal-bridge.test.cjs
  </read_first>
  <files>bin/lib/ljx-rebuttal-tools.cjs</files>
  <behavior>
    - `readRebuttalContext()` spreads `buildPaperWorkflowContext(projectRoot, baseContext, { venueConfirmed: !requiresVenueRuleConfirmation })` into `extraFields`.
    - `readRebuttalContext()` preserves `currentStage`, `reviewSources`, `rebuttalWorkspacePath`, `defaultVenue`, `responseMode`, `autoExperiment`, `characterLimit`, and recommendations.
    - `writeRebuttalState()` calls `validatePaperStateRecordForWrite()` before updating.
    - `writeRebuttalState()` uses `updateStateRecord()` and stores payload under `rebuttal` with `updatedBy: 'rebuttal'`, preserving existing `paperPipeline` and sibling fields.
    - CLI supports `context` and `write-rebuttal-state --payload-file`.
  </behavior>
  <action>
    Modify `bin/lib/ljx-rebuttal-tools.cjs` to import `buildPaperWorkflowContext` and `validatePaperStateRecordForWrite`. Add `writeRebuttalState(projectRoot, options = {})`. Add CLI parsing branch for `write-rebuttal-state` that mirrors the paper-pipeline payload-file pattern. Export `writeRebuttalState`. Do not change the missing-review honest stop except to include shared paper-state paths if the existing context branch can do so without hiding the stop.
  </action>
  <verify>
    <automated>node --check bin/lib/ljx-rebuttal-tools.cjs && node --test tests/rebuttal-bridge.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    Rebuttal shares the paper-state link model, preserves existing resume/stop behavior, respects venue confirmation, and writes state only through an explicit validated command.
  </acceptance_criteria>
  <done>`rebuttal` owns its bounded workspace and state linkage without a second submission control plane.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Tampering | Rebuttal paper-state update | mitigate | Use shared validation and field-preserving `updateStateRecord()`. |
| T-12-02-02 | Spoofing | Missing-review handling | mitigate | Preserve initial missing-review stop and workspace-resume distinction. |
| T-12-02-03 | Repudiation | Venue limit readiness | mitigate | Surface venue confirmation status explicitly in helper context and tests. |
| T-12-02-04 | Elevation of Privilege | Rebuttal-time experiments | mitigate | Keep experiment bridge as explicit recommendation unless configuration says otherwise; do not launch experiments in helper code. |
</threat_model>

<verification>
Run:
```bash
node --check bin/lib/ljx-rebuttal-tools.cjs
node --test tests/rebuttal-bridge.test.cjs
git diff --check
```
</verification>

<success_criteria>
Plan 12-02 is complete when `rebuttal` exposes shared paper-state context, preserves Auto rebuttal resume semantics, and writes only rebuttal-owned state through an explicit validated writer.
</success_criteria>

<output>
After completion, create `.planning/phases/12-complete-paper-and-rebuttal-workflows/12-02-SUMMARY.md`.
</output>
