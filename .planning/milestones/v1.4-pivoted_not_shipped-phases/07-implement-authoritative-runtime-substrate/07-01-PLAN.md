---
phase: "07-implement-authoritative-runtime-substrate"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "bin/lib/ljx-runtime-core.cjs"
  - "bin/lib/ljx-bridge-contract.cjs"
  - "bin/lib/ljx-state-tools.cjs"
  - "tests/runtime-core.test.cjs"
  - "tests/bridge-contract.test.cjs"
  - "tests/runtime-shell.test.cjs"
autonomous: true
requirements:
  - "IMPL-01"
user_setup: []
must_haves:
  truths:
    - "Shared runtime consumers resolve planning root, canonical config, typed phase identity, and active workstream from one module instead of duplicated helper logic."
    - "Structured state takes precedence when present, and resolver conflicts stop honestly instead of silently mutating more state."
    - "Phase-level workflow overrides come from canonical phase-record data rather than ad hoc per-command config parsing."
  artifacts:
    - "bin/lib/ljx-runtime-core.cjs exports the canonical config and typed-resolution substrate."
    - "tests/runtime-core.test.cjs proves config layering, phase resolution, workstream resolution, and conflict behavior."
  key_links:
    - "ljx-bridge-contract.cjs and ljx-state-tools.cjs consume the shared runtime core instead of reimplementing low-level resolution rules."
---

<objective>
Build the first shared runtime core for `ljx-GSD` so later lifecycle and research commands stop duplicating planning-root discovery, config loading, phase lookup, and workstream resolution.

Purpose: establish one reusable substrate before authoritative state writers and helper migration land.
Output: a new shared runtime-core module, minimal consumer migration in existing substrate-level helpers, and focused resolver tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-implement-authoritative-runtime-substrate/07-CONTEXT.md
@.planning/phases/07-implement-authoritative-runtime-substrate/07-RESEARCH.md
@LJX-GSD-CONFIGURATION-DESIGN.md
@LJX-GSD-ARCHITECTURE.md
@bin/lib/ljx-bridge-contract.cjs
@bin/lib/ljx-state-tools.cjs
@bin/lib/ljx-new-project-tools.cjs
@/Users/lijiaxin/.codex/get-shit-done/bin/lib/core.cjs
@/Users/lijiaxin/.codex/get-shit-done/bin/lib/init.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement canonical runtime-core helpers</name>
  <files>bin/lib/ljx-runtime-core.cjs</files>
  <read_first>bin/lib/ljx-state-tools.cjs, bin/lib/ljx-bridge-contract.cjs, /Users/lijiaxin/.codex/get-shit-done/bin/lib/core.cjs, LJX-GSD-CONFIGURATION-DESIGN.md</read_first>
  <action>Create `bin/lib/ljx-runtime-core.cjs` as the only low-level substrate for Phase 07. Export concrete helpers for: planning-root discovery, safe JSON reads, canonical config loading, phase-record lookup, typed phase resolution, and active-workstream resolution. Implement the accepted precedence exactly: structured state first, then markdown/pointer fallback only when structured state is absent, and honest stop metadata when structured and legacy sources conflict. Merge config in the order `safe defaults -> .planning/config.json -> phase-record overrides -> explicit runtime overrides`. Do not auto-backfill structured state during fallback reads. Keep the module CommonJS and reuse existing naming/style from `bin/lib/*.cjs`.</action>
  <verify>node --test tests/runtime-core.test.cjs</verify>
  <acceptance_criteria>
    - `bin/lib/ljx-runtime-core.cjs` exports canonical helpers for config and typed resolution.
    - Resolver behavior prefers `.planning/state/...` when present and exposes explicit conflict metadata instead of silent normalization.
    - Phase-record workflow overrides are applied without command-specific merge code.
  </acceptance_criteria>
  <done>The shared runtime core exists, is reusable, and passes focused tests for config and resolver semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint substrate-level helpers onto the runtime core</name>
  <files>bin/lib/ljx-bridge-contract.cjs, bin/lib/ljx-state-tools.cjs</files>
  <read_first>bin/lib/ljx-runtime-core.cjs, bin/lib/ljx-bridge-contract.cjs, bin/lib/ljx-state-tools.cjs, .planning/phases/07-implement-authoritative-runtime-substrate/07-CONTEXT.md</read_first>
  <action>Modify `ljx-bridge-contract.cjs` and `ljx-state-tools.cjs` so low-level planning-root, phase, and config resolution flows through the new runtime core instead of remaining duplicated or markdown-first. Preserve current public behavior, current honest-stop wording style, and current bridge-era domain policies such as code-review requirement defaults. Do not expand lifecycle semantics in this plan; the goal is only to move shared low-level logic underneath existing behavior.</action>
  <verify>node --test tests/bridge-contract.test.cjs tests/runtime-shell.test.cjs</verify>
  <acceptance_criteria>
    - `ljx-bridge-contract.cjs` still owns domain rules, not low-level repository resolution.
    - `ljx-state-tools.cjs` consumes canonical runtime-core helpers for planning and resolver behavior.
    - Existing runtime-shell tests remain green without changing accepted public recommendations.
  </acceptance_criteria>
  <done>Substrate-level helpers now depend on the runtime core while preserving current bridge contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for config layering and resolver conflicts</name>
  <files>tests/runtime-core.test.cjs, tests/bridge-contract.test.cjs, tests/runtime-shell.test.cjs</files>
  <read_first>tests/runtime-shell.test.cjs, tests/bridge-contract.test.cjs, bin/lib/ljx-runtime-core.cjs</read_first>
  <action>Add focused tests that cover: phase-record override merging, structured-state-first phase resolution, structured-state-first active-workstream resolution, stale legacy pointer handling, malformed JSON honest-stop behavior, and explicit conflict signaling when structured state disagrees with markdown-era sources. Follow the existing `node:test` style already used in bridge tests. Keep the assertions tied to accepted Phase 07 semantics, not speculative later lifecycle behavior.</action>
  <verify>node --test tests/runtime-core.test.cjs tests/bridge-contract.test.cjs tests/runtime-shell.test.cjs</verify>
  <acceptance_criteria>
    - Tests fail if helpers silently prefer fallback markdown over structured state.
    - Tests fail if conflicts are silently repaired or backfilled.
    - Tests demonstrate that the new substrate preserves current honest-stop behavior for malformed inputs.
  </acceptance_criteria>
  <done>Regression coverage makes the runtime-core contract safe to reuse in later plans.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/runtime-core.test.cjs`
- [ ] `node --test tests/bridge-contract.test.cjs tests/runtime-shell.test.cjs`
- [ ] `npm test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Shared runtime-core helpers replace duplicated low-level resolution logic without changing accepted bridge semantics
</success_criteria>

<output>
After completion, create `.planning/phases/07-implement-authoritative-runtime-substrate/07-01-SUMMARY.md`
</output>
