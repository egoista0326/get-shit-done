---
phase: "07-implement-authoritative-runtime-substrate"
plan: "02"
type: "execute"
wave: 2
depends_on:
  - "07-01"
files_modified:
  - "bin/lib/ljx-runtime-state.cjs"
  - "bin/lib/ljx-new-project-tools.cjs"
  - "bin/lib/ljx-state-tools.cjs"
  - "bin/lib/ljx-workstreams-tools.cjs"
  - "tests/runtime-state.test.cjs"
  - "tests/new-project-bridge.test.cjs"
  - "tests/workstreams-bridge.test.cjs"
  - "tests/runtime-shell.test.cjs"
autonomous: true
requirements:
  - "IMPL-01"
user_setup: []
must_haves:
  truths:
    - "The first authoritative families under `.planning/state/` can be created, read, and updated through one canonical IO layer."
    - "Primary workstream semantics are represented in structured state without turning legacy pointer files back into the default truth source."
    - "New-project and runtime-shell surfaces can seed and read authoritative state without reopening ownership boundaries."
  artifacts:
    - "bin/lib/ljx-runtime-state.cjs provides safe IO helpers for `phase-records`, `sessions`, and `workstreams`."
    - "tests/runtime-state.test.cjs proves schema-safe reads, writes, and fallback behavior for the first core families."
  key_links:
    - "ljx-new-project-tools.cjs seeds canonical state families that runtime-shell and workstreams helpers later read."
    - "ljx-workstreams-tools.cjs prefers structured workstream records before looking at legacy pointers."
---

<objective>
Implement the first authoritative structured-state IO layer for `phase-records`, `sessions`, and `workstreams`, then connect the repo's core bridge surfaces to that layer.

Purpose: make `.planning/state/` real for the first core runtime families before wider helper migration begins.
Output: canonical state IO helpers, seeded workstream/session-aware records, and regression tests around structured-state-first behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-implement-authoritative-runtime-substrate/07-CONTEXT.md
@.planning/phases/07-implement-authoritative-runtime-substrate/07-RESEARCH.md
@.planning/phases/05-migration-and-parallelism-strategy/05-02-PLAN.md
@LJX-GSD-ARCHITECTURE.md
@bin/lib/ljx-runtime-core.cjs
@bin/lib/ljx-new-project-tools.cjs
@bin/lib/ljx-state-tools.cjs
@bin/lib/ljx-workstreams-tools.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement canonical IO for phase-records, sessions, and workstreams</name>
  <files>bin/lib/ljx-runtime-state.cjs</files>
  <read_first>.planning/phases/05-migration-and-parallelism-strategy/05-02-PLAN.md, bin/lib/ljx-runtime-core.cjs, bin/lib/ljx-new-project-tools.cjs</read_first>
  <action>Create `bin/lib/ljx-runtime-state.cjs` with safe read/write/update helpers for the first three authoritative families: `phase-records`, `sessions`, and `workstreams`. The module should centralize canonical file paths, record initialization, schema versioning hooks, malformed JSON handling, and explicit fallback metadata. Keep writes idempotent and narrow: write only the fields each caller owns, and never let fallback markdown reads silently backfill authoritative state as a side effect. Model `primary` as a formal workstream record while preserving its accepted top-level mainline semantics.</action>
  <verify>node --test tests/runtime-state.test.cjs</verify>
  <acceptance_criteria>
    - `bin/lib/ljx-runtime-state.cjs` exposes reusable helpers for the three core families.
    - Reads return explicit malformed/missing/conflict states instead of throwing or silently repairing data.
    - Workstream records can represent `primary` explicitly without changing its accepted special semantics.
  </acceptance_criteria>
  <done>A canonical state IO layer exists for the first authoritative runtime families.</done>
</task>

<task type="auto">
  <name>Task 2: Seed and consume the new core state families from project init and runtime readers</name>
  <files>bin/lib/ljx-new-project-tools.cjs, bin/lib/ljx-state-tools.cjs, bin/lib/ljx-workstreams-tools.cjs</files>
  <read_first>bin/lib/ljx-runtime-state.cjs, bin/lib/ljx-new-project-tools.cjs, bin/lib/ljx-state-tools.cjs, bin/lib/ljx-workstreams-tools.cjs</read_first>
  <action>Update `ljx-new-project-tools.cjs` to seed the authoritative families needed at initialization time, at minimum `phase-records/{first_phase}.json` and `workstreams/primary.json`, plus any safe session scaffolding required by existing runtime-shell behavior. Update `ljx-state-tools.cjs` and `ljx-workstreams-tools.cjs` so they prefer structured-state reads for current phase and active workstream context, only consult legacy fallback files when authoritative records are absent, and stop honestly on structured-vs-legacy conflicts. Preserve current read-only workstream surface and do not implement deferred mutating operations in this plan.</action>
  <verify>node --test tests/new-project-bridge.test.cjs tests/workstreams-bridge.test.cjs tests/runtime-shell.test.cjs</verify>
  <acceptance_criteria>
    - New-project creates the authoritative seed records expected by the Phase 07 design.
    - Runtime-shell and workstreams readers prefer structured state when it exists.
    - Legacy pointer files remain fallback-only and do not silently override structured records.
  </acceptance_criteria>
  <done>The repo's core runtime readers operate against real `.planning/state` data for the first three families.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for seeded state and primary workstream behavior</name>
  <files>tests/runtime-state.test.cjs, tests/new-project-bridge.test.cjs, tests/workstreams-bridge.test.cjs, tests/runtime-shell.test.cjs</files>
  <read_first>tests/new-project-bridge.test.cjs, tests/workstreams-bridge.test.cjs, tests/runtime-shell.test.cjs, bin/lib/ljx-runtime-state.cjs</read_first>
  <action>Add or extend tests to prove: seeded `phase-records` and `workstreams/primary` files are created by project init, structured-state-first reads work for workstream status/progress, stale legacy pointer handling remains explicit, malformed state files stop honestly, and runtime-shell summaries stay stable when structured state exists. Keep tests small and phase-local; they should verify Phase 07 substrate behavior, not later lifecycle-shell features.</action>
  <verify>node --test tests/runtime-state.test.cjs tests/new-project-bridge.test.cjs tests/workstreams-bridge.test.cjs tests/runtime-shell.test.cjs</verify>
  <acceptance_criteria>
    - Tests fail if new-project omits required authoritative seed files.
    - Tests fail if workstreams falls back to pointer-era truth while authoritative records exist.
    - Tests document the accepted `primary` semantics in structured state form.
  </acceptance_criteria>
  <done>Core-family state behavior is locked by automated tests before wider helper migration begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/runtime-state.test.cjs`
- [ ] `node --test tests/new-project-bridge.test.cjs tests/workstreams-bridge.test.cjs tests/runtime-shell.test.cjs`
- [ ] `npm test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The first authoritative runtime families under `.planning/state/` are real, seeded, and consumed by core runtime readers
</success_criteria>

<output>
After completion, create `.planning/phases/07-implement-authoritative-runtime-substrate/07-02-SUMMARY.md`
</output>
