# Phase 22 Plan: v1.4 Self-Contained Runtime And Scenario Review

## Objective

Execute the v1.4 protocol in `.planning/review/v1.4/PROTOCOL.md` without skipping second-pass confirmation, bug ledger, regular review, scenario review, or verification evidence.

## Tasks

- [x] Create the v1.4 review protocol, review state, bug ledger, context, and plan.
- [x] Run Stage A Round 1 regular review with focused subagents and local scans.
- [x] Confirm or reject each Stage A Round 1 candidate.
- [x] Fix accepted Stage A issues minimally while preserving GSD/Auto/ARIS capability inside the `ljx-GSD` closed loop.
- [x] Record accepted issues in `.planning/review/v1.4/BUG-LEDGER.md`.
- [x] Run targeted and full verification after fixes.
- [x] Continue Stage A until two consecutive clean rounds or the 10-round cap.
- [x] Run Stage B scenario review after Stage A exits.
- [x] In Stage B, install/use `ljx-GSD` in temporary scenarios and require subagents to report exact `ljx-GSD-*` skill chains, artifacts, config values, raw-skill usage, and cleanup status.
- [x] Complete Stage B Round 1 as fixed_not_clean, fix accepted V14-154 through V14-156, and update bug ledger/review state.
- [ ] Continue Stage B until two consecutive clean scenario rounds or the 10-round cap.

## Verification

Minimum post-fix verification:

- relevant targeted tests for touched families
- `node bin/install.js --preview`
- generated `ljx-GSD-*` raw invocation scan
- independent packaged-path/global-dependency scan
- `node --test tests/docs-contract.test.cjs`
- full `node --test tests/*.test.cjs` before stage/round closure when feasible
- `git diff --check`
