---
phase: 13-complete-workstream-and-roadmap-mutation-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/ljx-workstreams-tools.cjs
  - tests/workstreams-bridge.test.cjs
  - .planning/phases/13-complete-workstream-and-roadmap-mutation-admin/13-01-SUMMARY.md
autonomous: true
requirements:
  - IMPL-07
requirements_addressed:
  - IMPL-07
user_setup: []
---

<objective>
Complete mutating `workstreams` semantics while preserving the accepted primary/mainline boundary.

Purpose: Phase 13 needs logical workstream mutation to become real without turning workstreams into physical workspaces, generic merge-back, or a second primary mainline.

Output: `ljx-workstreams-tools.cjs` performs safe `create` / `switch` / `resume` / `complete` mutations, tests lock primary and active-pointer safety, and `13-01-SUMMARY.md` records what changed.
</objective>

<execution_context>
@/Users/lijiaxin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lijiaxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/IMPLEMENTATION-LESSONS.md
@.planning/phases/13-complete-workstream-and-roadmap-mutation-admin/13-CONTEXT.md
@.planning/phases/13-complete-workstream-and-roadmap-mutation-admin/13-RESEARCH.md
@bin/lib/ljx-workstreams-tools.cjs
@bin/lib/ljx-runtime-core.cjs
@bin/lib/ljx-runtime-state.cjs
@tests/workstreams-bridge.test.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify safe workstream mutation behavior</name>
  <read_first>
    .planning/phases/13-complete-workstream-and-roadmap-mutation-admin/13-CONTEXT.md
    .planning/phases/13-complete-workstream-and-roadmap-mutation-admin/13-RESEARCH.md
    bin/lib/ljx-workstreams-tools.cjs
    bin/lib/ljx-runtime-core.cjs
    bin/lib/ljx-runtime-state.cjs
    tests/workstreams-bridge.test.cjs
  </read_first>
  <files>tests/workstreams-bridge.test.cjs</files>
  <behavior>
    - `create <name>` creates `.planning/state/workstreams/<name>.json`, rejects empty/path/traversal names, rejects `primary`, and rejects collisions with structured records or legacy workstream directories.
    - `switch primary` activates the structured primary record and the `.planning/active-workstream` mirror without creating `.planning/workstreams/primary/`.
    - `switch <name>` and `resume <name>` activate exactly one requested workstream and return next-action guidance without claiming lifecycle execution already ran.
    - `complete <name>` refuses `primary`, missing workstreams, immutable workstreams, and already-complete workstreams; it marks mutable secondaries completed/archived.
    - Legacy `.planning/workstreams/<name>/` directories remain visible for read compatibility and are not deleted by mutation commands.
    - Malformed `.planning/state/workstreams`, invalid JSON, stale active pointers, and structured/legacy pointer conflicts stop before mutation.
  </behavior>
  <action>
    Add failing tests first. Assert returned JSON plus filesystem state: structured records, active flags, `.planning/active-workstream` mirror, absence of `.planning/workstreams/primary/`, no deletion of legacy directories, and stop reason codes such as `invalid_workstream_name`, `reserved_workstream_name`, `workstream_collision`, `missing_workstream`, `primary_destructive_operation`, `workstream_already_complete`, `invalid_workstream_record_json`, `invalid_state_family_path`, and `workstream_resolution_conflict`.
  </action>
  <verify>
    <automated>node --test tests/workstreams-bridge.test.cjs</automated>
  </verify>
  <done>Workstream mutation requirements are encoded as executable tests before helper behavior changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement structured workstream mutations</name>
  <read_first>
    bin/lib/ljx-workstreams-tools.cjs
    bin/lib/ljx-runtime-core.cjs
    bin/lib/ljx-runtime-state.cjs
    tests/workstreams-bridge.test.cjs
  </read_first>
  <files>bin/lib/ljx-workstreams-tools.cjs</files>
  <behavior>
    - `mutateWorkstream(projectRoot, { subcommand, name })` performs `create` / `switch` / `resume` / `complete` with structured JSON output.
    - New writes use `.planning/state/workstreams/<name>.json` and `ensurePrimaryWorkstreamRecord()`.
    - Active state has exactly one structured active record and mirrors `.planning/active-workstream` only to keep `resolveActiveWorkstream()` unambiguous.
    - Mutations stop before writes if `resolveActiveWorkstream()` reports stale or conflicting state.
    - Mutation records include command, target, previous status where applicable, timestamp, and stop/success provenance inside the affected record.
  </behavior>
  <action>
    Extend `bin/lib/ljx-workstreams-tools.cjs` only. Keep existing read-only `list/status/progress` paths intact. Add safe name validation, collision checks against structured records and legacy dirs, and helpers to update structured workstream records so exactly one has `active: true`. For `complete`, set a terminal status such as `completed`; if the completed workstream was active, return active state to `primary`. Return adoption/import only as recommendations, never as merge-back behavior.
  </action>
  <verify>
    <automated>node --check bin/lib/ljx-workstreams-tools.cjs && node --test tests/workstreams-bridge.test.cjs</automated>
  </verify>
  <done>`workstreams create/switch/resume/complete` are bridge-ready helper operations for IMPL-07.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire CLI context output and read-after-write consistency</name>
  <read_first>
    bin/lib/ljx-workstreams-tools.cjs
    tests/workstreams-bridge.test.cjs
  </read_first>
  <files>bin/lib/ljx-workstreams-tools.cjs, tests/workstreams-bridge.test.cjs</files>
  <behavior>
    - CLI invocation `node bin/lib/ljx-workstreams-tools.cjs context --cwd <tmp> create hypothesis-b` returns `bridgeReady: true`, `recordsWritten`, `activeWorkstream`, and recommendations.
    - Follow-up `list`, `status`, and `progress` reflect the mutation.
    - Unsupported subcommands still return `unsupported_subcommand`.
    - Missing `.planning/` still routes to `ljx-GSD-new-project`.
  </behavior>
  <action>
    Update `readWorkstreamsContext()` so mutating subcommands call `mutateWorkstream()` instead of the old stop response. Add CLI-focused assertions using `child_process.execFileSync` only for helper-level JSON output.
  </action>
  <verify>
    <automated>node --check bin/lib/ljx-workstreams-tools.cjs && node --test tests/workstreams-bridge.test.cjs</automated>
  </verify>
  <done>Mutating and read-only workstream helper paths agree after create/switch/resume/complete.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Tampering | `.planning/state/workstreams/*.json` | mitigate | Validate names, reject collisions and malformed state, and write through runtime-state helpers. |
| T-13-01-02 | Tampering | Active workstream pointer | mitigate | Set exactly one structured active record and mirror `.planning/active-workstream` consistently. |
| T-13-01-03 | Elevation of Privilege | `primary` workstream | mitigate | Reject destructive or identity-changing primary operations. |
| T-13-01-04 | Repudiation | Workstream mutation provenance | mitigate | Store bounded mutation provenance and return `recordsWritten`. |
</threat_model>

<verification>
Run:
```bash
node --check bin/lib/ljx-workstreams-tools.cjs
node --test tests/workstreams-bridge.test.cjs
git diff --check
```

Do not mark Phase 12, Phase 13, IMPL-06, or IMPL-07 complete.
</verification>

<success_criteria>
Plan 13-01 is complete when `workstreams create/switch/resume/complete` safely mutate structured workstream state, primary semantics are protected, legacy workstream read compatibility remains intact, and helper tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/13-complete-workstream-and-roadmap-mutation-admin/13-01-SUMMARY.md`.
</output>
